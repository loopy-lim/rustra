// This file was autogenerated by some hot garbage in the `uniffi` crate.
// Trust me, you don't want to mess with it!

// swiftlint:disable all
import Foundation

// Depending on the consumer's build setup, the low-level FFI code
// might be in a separate module, or it might be compiled inline into
// this module. This is a bit of light hackery to work with both.
#if canImport(rustra_calculator_exampleFFI)
import rustra_calculator_exampleFFI
#endif

fileprivate extension RustBuffer {
    // Allocate a new buffer, copying the contents of a `UInt8` array.
    init(bytes: [UInt8]) {
        let rbuf = bytes.withUnsafeBufferPointer { ptr in
            RustBuffer.from(ptr)
        }
        self.init(capacity: rbuf.capacity, len: rbuf.len, data: rbuf.data)
    }

    static func empty() -> RustBuffer {
        RustBuffer(capacity: 0, len:0, data: nil)
    }

    static func from(_ ptr: UnsafeBufferPointer<UInt8>) -> RustBuffer {
        try! rustCall { ffi_rustra_calculator_example_rustbuffer_from_bytes(ForeignBytes(bufferPointer: ptr), $0) }
    }

    // Frees the buffer in place.
    // The buffer must not be used after this is called.
    func deallocate() {
        try! rustCall { ffi_rustra_calculator_example_rustbuffer_free(self, $0) }
    }
}

fileprivate extension ForeignBytes {
    init(bufferPointer: UnsafeBufferPointer<UInt8>) {
        self.init(len: Int32(bufferPointer.count), data: bufferPointer.baseAddress)
    }

    init(rawBufferPointer: UnsafeRawBufferPointer) {
        self.init(
            len: Int32(rawBufferPointer.count),
            data: rawBufferPointer.baseAddress?.assumingMemoryBound(to: UInt8.self)
        )
    }
}

// Converter for `&[u8]` / `[ByRef] bytes` arguments.
//
// Conforms to `FfiConverter` so the compiler enforces the full converter
// method set. Only the scope-bound `lower(_:_body:)` overload is sound —
// zero-copy byte buffers only flow foreign -> Rust, and only in argument
// position. The four protocol-witness methods (`lift`, `lower`, `read`,
// `write`) `fatalError` at runtime if anyone reaches them.
//
// The scope-bound `lower` takes a closure because the `ForeignBytes`
// pointer is only guaranteed valid for the duration of
// `Data.withUnsafeBytes`. Callers must run the full FFI call inside
// the closure body.
fileprivate enum FfiConverterByRefBytes: FfiConverter {
    typealias SwiftType = Data
    typealias FfiType = ForeignBytes

    static func lower<R>(_ value: Data, _ body: (ForeignBytes) throws -> R) rethrows -> R {
        return try value.withUnsafeBytes { rawBuf in
            try body(ForeignBytes(rawBufferPointer: rawBuf))
        }
    }

    static func lower(_ value: Data) -> ForeignBytes {
        fatalError("ByRef bytes cannot use the plain lower: returning ForeignBytes escapes the Data.withUnsafeBytes scope. Use the scope-bound lower(_:_body:) overload instead.")
    }

    static func lift(_ value: ForeignBytes) throws -> Data {
        fatalError("ByRef bytes cannot be lifted: zero-copy &[u8] only flows foreign->Rust")
    }

    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Data {
        fatalError("ByRef bytes cannot be read from a buffer: zero-copy &[u8] is only supported in argument position, not nested in records/options/etc.")
    }

    static func write(_ value: Data, into buf: inout [UInt8]) {
        fatalError("ByRef bytes cannot be written to a buffer: zero-copy &[u8] is only supported in argument position, not nested in records/options/etc.")
    }
}

// For every type used in the interface, we provide helper methods for conveniently
// lifting and lowering that type from C-compatible data, and for reading and writing
// values of that type in a buffer.

// Helper classes/extensions that don't change.
// Someday, this will be in a library of its own.

fileprivate extension Data {
    init(rustBuffer: RustBuffer) {
        self.init(
            bytesNoCopy: rustBuffer.data!,
            count: Int(rustBuffer.len),
            deallocator: .none
        )
    }
}

// Define reader functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.
//
// With external types, one swift source file needs to be able to call the read
// method on another source file's FfiConverter, but then what visibility
// should Reader have?
// - If Reader is fileprivate, then this means the read() must also
//   be fileprivate, which doesn't work with external types.
// - If Reader is internal/public, we'll get compile errors since both source
//   files will try define the same type.
//
// Instead, the read() method and these helper functions input a tuple of data

fileprivate func createReader(data: Data) -> (data: Data, offset: Data.Index) {
    (data: data, offset: 0)
}

// Reads an integer at the current offset, in big-endian order, and advances
// the offset on success. Throws if reading the integer would move the
// offset past the end of the buffer.
fileprivate func readInt<T: FixedWidthInteger>(_ reader: inout (data: Data, offset: Data.Index)) throws -> T {
    let range = reader.offset..<reader.offset + MemoryLayout<T>.size
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    if T.self == UInt8.self {
        let value = reader.data[reader.offset]
        reader.offset += 1
        return value as! T
    }
    var value: T = 0
    let _ = withUnsafeMutableBytes(of: &value, { reader.data.copyBytes(to: $0, from: range)})
    reader.offset = range.upperBound
    return value.bigEndian
}

// Reads an arbitrary number of bytes, to be used to read
// raw bytes, this is useful when lifting strings
fileprivate func readBytes(_ reader: inout (data: Data, offset: Data.Index), count: Int) throws -> Array<UInt8> {
    let range = reader.offset..<(reader.offset+count)
    guard reader.data.count >= range.upperBound else {
        throw UniffiInternalError.bufferOverflow
    }
    var value = [UInt8](repeating: 0, count: count)
    value.withUnsafeMutableBufferPointer({ buffer in
        reader.data.copyBytes(to: buffer, from: range)
    })
    reader.offset = range.upperBound
    return value
}

// Reads a float at the current offset.
fileprivate func readFloat(_ reader: inout (data: Data, offset: Data.Index)) throws -> Float {
    return Float(bitPattern: try readInt(&reader))
}

// Reads a float at the current offset.
fileprivate func readDouble(_ reader: inout (data: Data, offset: Data.Index)) throws -> Double {
    return Double(bitPattern: try readInt(&reader))
}

// Indicates if the offset has reached the end of the buffer.
fileprivate func hasRemaining(_ reader: (data: Data, offset: Data.Index)) -> Bool {
    return reader.offset < reader.data.count
}

// Define writer functionality.  Normally this would be defined in a class or
// struct, but we use standalone functions instead in order to make external
// types work.  See the above discussion on Readers for details.

fileprivate func createWriter() -> [UInt8] {
    return []
}

fileprivate func writeBytes<S>(_ writer: inout [UInt8], _ byteArr: S) where S: Sequence, S.Element == UInt8 {
    writer.append(contentsOf: byteArr)
}

// Writes an integer in big-endian order.
//
// Warning: make sure what you are trying to write
// is in the correct type!
fileprivate func writeInt<T: FixedWidthInteger>(_ writer: inout [UInt8], _ value: T) {
    var value = value.bigEndian
    withUnsafeBytes(of: &value) { writer.append(contentsOf: $0) }
}

fileprivate func writeFloat(_ writer: inout [UInt8], _ value: Float) {
    writeInt(&writer, value.bitPattern)
}

fileprivate func writeDouble(_ writer: inout [UInt8], _ value: Double) {
    writeInt(&writer, value.bitPattern)
}

// Protocol for types that transfer other types across the FFI. This is
// analogous to the Rust trait of the same name.
fileprivate protocol FfiConverter {
    associatedtype FfiType
    associatedtype SwiftType

    static func lift(_ value: FfiType) throws -> SwiftType
    static func lower(_ value: SwiftType) -> FfiType
    static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType
    static func write(_ value: SwiftType, into buf: inout [UInt8])
}

// Types conforming to `Primitive` pass themselves directly over the FFI.
fileprivate protocol FfiConverterPrimitive: FfiConverter where FfiType == SwiftType { }

extension FfiConverterPrimitive {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ value: FfiType) throws -> SwiftType {
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> FfiType {
        return value
    }
}

// Types conforming to `FfiConverterRustBuffer` lift and lower into a `RustBuffer`.
// Used for complex types where it's hard to write a custom lift/lower.
fileprivate protocol FfiConverterRustBuffer: FfiConverter where FfiType == RustBuffer {}

extension FfiConverterRustBuffer {
#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lift(_ buf: RustBuffer) throws -> SwiftType {
        var reader = createReader(data: Data(rustBuffer: buf))
        let value = try read(from: &reader)
        if hasRemaining(reader) {
            throw UniffiInternalError.incompleteData
        }
        buf.deallocate()
        return value
    }

#if swift(>=5.8)
    @_documentation(visibility: private)
#endif
    public static func lower(_ value: SwiftType) -> RustBuffer {
          var writer = createWriter()
          write(value, into: &writer)
          return RustBuffer(bytes: writer)
    }
}
// An error type for FFI errors. These errors occur at the UniFFI level, not
// the library level.
fileprivate enum UniffiInternalError: LocalizedError {
    case bufferOverflow
    case incompleteData
    case unexpectedOptionalTag
    case unexpectedEnumCase
    case unexpectedNullPointer
    case unexpectedRustCallStatusCode
    case unexpectedRustCallError
    case unexpectedStaleHandle
    case rustPanic(_ message: String)

    public var errorDescription: String? {
        switch self {
        case .bufferOverflow: return "Reading the requested value would read past the end of the buffer"
        case .incompleteData: return "The buffer still has data after lifting its containing value"
        case .unexpectedOptionalTag: return "Unexpected optional tag; should be 0 or 1"
        case .unexpectedEnumCase: return "Raw enum value doesn't match any cases"
        case .unexpectedNullPointer: return "Raw pointer value was null"
        case .unexpectedRustCallStatusCode: return "Unexpected RustCallStatus code"
        case .unexpectedRustCallError: return "CALL_ERROR but no errorClass specified"
        case .unexpectedStaleHandle: return "The object in the handle map has been dropped already"
        case let .rustPanic(message): return message
        }
    }
}

fileprivate extension NSLock {
    func withLock<T>(f: () throws -> T) rethrows -> T {
        self.lock()
        defer { self.unlock() }
        return try f()
    }
}

fileprivate let CALL_SUCCESS: Int8 = 0
fileprivate let CALL_ERROR: Int8 = 1
fileprivate let CALL_UNEXPECTED_ERROR: Int8 = 2
fileprivate let CALL_CANCELLED: Int8 = 3

fileprivate extension RustCallStatus {
    init() {
        self.init(
            code: CALL_SUCCESS,
            errorBuf: RustBuffer.init(
                capacity: 0,
                len: 0,
                data: nil
            )
        )
    }
}

private func rustCall<T>(_ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    let neverThrow: ((RustBuffer) throws -> Never)? = nil
    return try makeRustCall(callback, errorHandler: neverThrow)
}

private func rustCallWithError<T, E: Swift.Error>(
    _ errorHandler: @escaping (RustBuffer) throws -> E,
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T) throws -> T {
    try makeRustCall(callback, errorHandler: errorHandler)
}

private func makeRustCall<T, E: Swift.Error>(
    _ callback: (UnsafeMutablePointer<RustCallStatus>) -> T,
    errorHandler: ((RustBuffer) throws -> E)?
) throws -> T {
    uniffiEnsureRustraCalculatorExampleInitialized()
    var callStatus = RustCallStatus.init()
    let returnedVal = callback(&callStatus)
    try uniffiCheckCallStatus(callStatus: callStatus, errorHandler: errorHandler)
    return returnedVal
}

private func uniffiCheckCallStatus<E: Swift.Error>(
    callStatus: RustCallStatus,
    errorHandler: ((RustBuffer) throws -> E)?
) throws {
    switch callStatus.code {
        case CALL_SUCCESS:
            return

        case CALL_ERROR:
            if let errorHandler = errorHandler {
                throw try errorHandler(callStatus.errorBuf)
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.unexpectedRustCallError
            }

        case CALL_UNEXPECTED_ERROR:
            // When the rust code sees a panic, it tries to construct a RustBuffer
            // with the message.  But if that code panics, then it just sends back
            // an empty buffer.
            if callStatus.errorBuf.len > 0 {
                throw UniffiInternalError.rustPanic(try FfiConverterString.lift(callStatus.errorBuf))
            } else {
                callStatus.errorBuf.deallocate()
                throw UniffiInternalError.rustPanic("Rust panic")
            }

        case CALL_CANCELLED:
            fatalError("Cancellation not supported yet")

        default:
            throw UniffiInternalError.unexpectedRustCallStatusCode
    }
}

private func uniffiTraitInterfaceCall<T>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> ()
) {
    do {
        try writeReturn(makeCall())
    } catch let error {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}

private func uniffiTraitInterfaceCallWithError<T, E>(
    callStatus: UnsafeMutablePointer<RustCallStatus>,
    makeCall: () throws -> T,
    writeReturn: (T) -> (),
    lowerError: (E) -> RustBuffer
) {
    do {
        try writeReturn(makeCall())
    } catch let error as E {
        callStatus.pointee.code = CALL_ERROR
        callStatus.pointee.errorBuf = lowerError(error)
    } catch {
        callStatus.pointee.code = CALL_UNEXPECTED_ERROR
        callStatus.pointee.errorBuf = FfiConverterString.lower(String(describing: error))
    }
}
// Initial value and increment amount for handles. 
// These ensure that SWIFT handles always have the lowest bit set
fileprivate let UNIFFI_HANDLEMAP_INITIAL: UInt64 = 1
fileprivate let UNIFFI_HANDLEMAP_DELTA: UInt64 = 2

fileprivate final class UniffiHandleMap<T>: @unchecked Sendable {
    // All mutation happens with this lock held, which is why we implement @unchecked Sendable.
    private let lock = NSLock()
    private var map: [UInt64: T] = [:]
    private var currentHandle: UInt64 = UNIFFI_HANDLEMAP_INITIAL

    func insert(obj: T) -> UInt64 {
        lock.withLock {
            return doInsert(obj)
        }
    }

    // Low-level insert function, this assumes `lock` is held.
    private func doInsert(_ obj: T) -> UInt64 {
        let handle = currentHandle
        currentHandle += UNIFFI_HANDLEMAP_DELTA
        map[handle] = obj
        return handle
    }

     func get(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

     func clone(handle: UInt64) throws -> UInt64 {
        try lock.withLock {
            guard let obj = map[handle] else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return doInsert(obj)
        }
    }

    @discardableResult
    func remove(handle: UInt64) throws -> T {
        try lock.withLock {
            guard let obj = map.removeValue(forKey: handle) else {
                throw UniffiInternalError.unexpectedStaleHandle
            }
            return obj
        }
    }

    var count: Int {
        get {
            map.count
        }
    }
}


// Public interface members begin here.


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt32: FfiConverterPrimitive {
    typealias FfiType = UInt32
    typealias SwiftType = UInt32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt32: FfiConverterPrimitive {
    typealias FfiType = Int32
    typealias SwiftType = Int32

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int32 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int32, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterUInt64: FfiConverterPrimitive {
    typealias FfiType = UInt64
    typealias SwiftType = UInt64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> UInt64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterInt64: FfiConverterPrimitive {
    typealias FfiType = Int64
    typealias SwiftType = Int64

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Int64 {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Int64, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDouble: FfiConverterPrimitive {
    typealias FfiType = Double
    typealias SwiftType = Double

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Double {
        return try lift(readDouble(&buf))
    }

    public static func write(_ value: Double, into buf: inout [UInt8]) {
        writeDouble(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterBool : FfiConverter {
    typealias FfiType = Int8
    typealias SwiftType = Bool

    public static func lift(_ value: Int8) throws -> Bool {
        return value != 0
    }

    public static func lower(_ value: Bool) -> Int8 {
        return value ? 1 : 0
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Bool {
        return try lift(readInt(&buf))
    }

    public static func write(_ value: Bool, into buf: inout [UInt8]) {
        writeInt(&buf, lower(value))
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterString: FfiConverter {
    typealias SwiftType = String
    typealias FfiType = RustBuffer

    public static func lift(_ value: RustBuffer) throws -> String {
        defer {
            value.deallocate()
        }
        if value.data == nil {
            return String()
        }
        let bytes = UnsafeBufferPointer<UInt8>(start: value.data!, count: Int(value.len))
        // Use Swift's native UTF-8 decoder; `String(bytes:encoding:.utf8)` goes
        // through Foundation's NSString and silently strips a leading U+FEFF BOM.
        // Invalid UTF-8 substitutes U+FFFD instead of trapping (unreachable
        // given Rust's `String` invariant).
        return String(decoding: bytes, as: UTF8.self)
    }

    public static func lower(_ value: String) -> RustBuffer {
        return value.utf8CString.withUnsafeBufferPointer { ptr in
            // The swift string gives us int8_t, we want uint8_t.
            ptr.withMemoryRebound(to: UInt8.self) { ptr in
                // The swift string gives us a trailing null byte, we don't want it.
                let buf = UnsafeBufferPointer(rebasing: ptr.prefix(upTo: ptr.count - 1))
                return RustBuffer.from(buf)
            }
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> String {
        let len: Int32 = try readInt(&buf)
        // See `lift` above for why we avoid Foundation's NSString-backed decoder here.
        return String(decoding: try readBytes(&buf, count: Int(len)), as: UTF8.self)
    }

    public static func write(_ value: String, into buf: inout [UInt8]) {
        let len = Int32(value.utf8.count)
        writeInt(&buf, len)
        writeBytes(&buf, value.utf8)
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterData: FfiConverterRustBuffer {
    typealias SwiftType = Data

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Data {
        let len: Int32 = try readInt(&buf)
        return Data(try readBytes(&buf, count: Int(len)))
    }

    public static func write(_ value: Data, into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        writeBytes(&buf, value)
    }
}


/**
 * `AddNumbersInput` 미러 — 실제 타입 `crate::AddNumbersInput`.
 */
public struct AddNumbersInput: Equatable, Hashable {
    public var a: Int64
    public var b: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(a: Int64, b: Int64) {
        self.a = a
        self.b = b
    }

    

    
}

#if compiler(>=6)
extension AddNumbersInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAddNumbersInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AddNumbersInput {
        return
            try AddNumbersInput(
                a: FfiConverterInt64.read(from: &buf), 
                b: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: AddNumbersInput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.a, into: &buf)
        FfiConverterInt64.write(value.b, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAddNumbersInput_lift(_ buf: RustBuffer) throws -> AddNumbersInput {
    return try FfiConverterTypeAddNumbersInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAddNumbersInput_lower(_ value: AddNumbersInput) -> RustBuffer {
    return FfiConverterTypeAddNumbersInput.lower(value)
}


/**
 * `AddNumbersOutput` 미러 — 실제 타입 `crate::AddNumbersOutput`.
 */
public struct AddNumbersOutput: Equatable, Hashable {
    public var value: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: Int64) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension AddNumbersOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeAddNumbersOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> AddNumbersOutput {
        return
            try AddNumbersOutput(
                value: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: AddNumbersOutput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAddNumbersOutput_lift(_ buf: RustBuffer) throws -> AddNumbersOutput {
    return try FfiConverterTypeAddNumbersOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeAddNumbersOutput_lower(_ value: AddNumbersOutput) -> RustBuffer {
    return FfiConverterTypeAddNumbersOutput.lower(value)
}


/**
 * `BenchAddInput` 미러 — 실제 타입 `crate::BenchAddInput`.
 */
public struct BenchAddInput: Equatable, Hashable {
    public var a: Double
    public var b: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(a: Double, b: Double) {
        self.a = a
        self.b = b
    }

    

    
}

#if compiler(>=6)
extension BenchAddInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBenchAddInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BenchAddInput {
        return
            try BenchAddInput(
                a: FfiConverterDouble.read(from: &buf), 
                b: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: BenchAddInput, into buf: inout [UInt8]) {
        FfiConverterDouble.write(value.a, into: &buf)
        FfiConverterDouble.write(value.b, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchAddInput_lift(_ buf: RustBuffer) throws -> BenchAddInput {
    return try FfiConverterTypeBenchAddInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchAddInput_lower(_ value: BenchAddInput) -> RustBuffer {
    return FfiConverterTypeBenchAddInput.lower(value)
}


/**
 * `BenchAddOutput` 미러 — 실제 타입 `crate::BenchAddOutput`.
 */
public struct BenchAddOutput: Equatable, Hashable {
    public var value: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: Double) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension BenchAddOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBenchAddOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BenchAddOutput {
        return
            try BenchAddOutput(
                value: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: BenchAddOutput, into buf: inout [UInt8]) {
        FfiConverterDouble.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchAddOutput_lift(_ buf: RustBuffer) throws -> BenchAddOutput {
    return try FfiConverterTypeBenchAddOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchAddOutput_lower(_ value: BenchAddOutput) -> RustBuffer {
    return FfiConverterTypeBenchAddOutput.lower(value)
}


/**
 * `BenchBytesPayload` 미러 — 실제 타입 `crate::BenchBytesPayload`.
 */
public struct BenchBytesPayload: Equatable, Hashable {
    public var data: Data

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(data: Data) {
        self.data = data
    }

    

    
}

#if compiler(>=6)
extension BenchBytesPayload: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBenchBytesPayload: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BenchBytesPayload {
        return
            try BenchBytesPayload(
                data: FfiConverterData.read(from: &buf)
        )
    }

    public static func write(_ value: BenchBytesPayload, into buf: inout [UInt8]) {
        FfiConverterData.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchBytesPayload_lift(_ buf: RustBuffer) throws -> BenchBytesPayload {
    return try FfiConverterTypeBenchBytesPayload.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchBytesPayload_lower(_ value: BenchBytesPayload) -> RustBuffer {
    return FfiConverterTypeBenchBytesPayload.lower(value)
}


/**
 * `BenchPairPayload` 미러 — 실제 타입 `crate::BenchPairPayload`.
 */
public struct BenchPairPayload: Equatable, Hashable {
    public var name: String
    public var value: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(name: String, value: Double) {
        self.name = name
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension BenchPairPayload: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBenchPairPayload: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BenchPairPayload {
        return
            try BenchPairPayload(
                name: FfiConverterString.read(from: &buf), 
                value: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: BenchPairPayload, into buf: inout [UInt8]) {
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterDouble.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchPairPayload_lift(_ buf: RustBuffer) throws -> BenchPairPayload {
    return try FfiConverterTypeBenchPairPayload.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchPairPayload_lower(_ value: BenchPairPayload) -> RustBuffer {
    return FfiConverterTypeBenchPairPayload.lower(value)
}


/**
 * `BenchStringPayload` 미러 — 실제 타입 `crate::BenchStringPayload`.
 */
public struct BenchStringPayload: Equatable, Hashable {
    public var value: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: String) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension BenchStringPayload: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeBenchStringPayload: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> BenchStringPayload {
        return
            try BenchStringPayload(
                value: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: BenchStringPayload, into buf: inout [UInt8]) {
        FfiConverterString.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchStringPayload_lift(_ buf: RustBuffer) throws -> BenchStringPayload {
    return try FfiConverterTypeBenchStringPayload.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeBenchStringPayload_lower(_ value: BenchStringPayload) -> RustBuffer {
    return FfiConverterTypeBenchStringPayload.lower(value)
}


/**
 * `ChannelDemoBytesInput` 미러 — 실제 타입 `crate::ChannelDemoBytesInput`.
 */
public struct ChannelDemoBytesInput: Equatable, Hashable {
    public var channel: UInt32
    public var ticks: Int32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(channel: UInt32, ticks: Int32) {
        self.channel = channel
        self.ticks = ticks
    }

    

    
}

#if compiler(>=6)
extension ChannelDemoBytesInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChannelDemoBytesInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChannelDemoBytesInput {
        return
            try ChannelDemoBytesInput(
                channel: FfiConverterUInt32.read(from: &buf), 
                ticks: FfiConverterInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ChannelDemoBytesInput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.channel, into: &buf)
        FfiConverterInt32.write(value.ticks, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoBytesInput_lift(_ buf: RustBuffer) throws -> ChannelDemoBytesInput {
    return try FfiConverterTypeChannelDemoBytesInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoBytesInput_lower(_ value: ChannelDemoBytesInput) -> RustBuffer {
    return FfiConverterTypeChannelDemoBytesInput.lower(value)
}


/**
 * `ChannelDemoBytesOutput` 미러 — 실제 타입 `crate::ChannelDemoBytesOutput`.
 */
public struct ChannelDemoBytesOutput: Equatable, Hashable {
    public var sent: UInt32
    public var droppedSends: UInt32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(sent: UInt32, droppedSends: UInt32) {
        self.sent = sent
        self.droppedSends = droppedSends
    }

    

    
}

#if compiler(>=6)
extension ChannelDemoBytesOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChannelDemoBytesOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChannelDemoBytesOutput {
        return
            try ChannelDemoBytesOutput(
                sent: FfiConverterUInt32.read(from: &buf), 
                droppedSends: FfiConverterUInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ChannelDemoBytesOutput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.sent, into: &buf)
        FfiConverterUInt32.write(value.droppedSends, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoBytesOutput_lift(_ buf: RustBuffer) throws -> ChannelDemoBytesOutput {
    return try FfiConverterTypeChannelDemoBytesOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoBytesOutput_lower(_ value: ChannelDemoBytesOutput) -> RustBuffer {
    return FfiConverterTypeChannelDemoBytesOutput.lower(value)
}


/**
 * `ChannelDemoInput` 미러 — 실제 타입 `crate::ChannelDemoInput`.
 */
public struct ChannelDemoInput: Equatable, Hashable {
    public var channel: UInt32
    public var ticks: Int32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(channel: UInt32, ticks: Int32) {
        self.channel = channel
        self.ticks = ticks
    }

    

    
}

#if compiler(>=6)
extension ChannelDemoInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChannelDemoInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChannelDemoInput {
        return
            try ChannelDemoInput(
                channel: FfiConverterUInt32.read(from: &buf), 
                ticks: FfiConverterInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ChannelDemoInput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.channel, into: &buf)
        FfiConverterInt32.write(value.ticks, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoInput_lift(_ buf: RustBuffer) throws -> ChannelDemoInput {
    return try FfiConverterTypeChannelDemoInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoInput_lower(_ value: ChannelDemoInput) -> RustBuffer {
    return FfiConverterTypeChannelDemoInput.lower(value)
}


/**
 * `ChannelDemoOutput` 미러 — 실제 타입 `crate::ChannelDemoOutput`.
 */
public struct ChannelDemoOutput: Equatable, Hashable {
    public var sent: Int32
    public var droppedSends: Int32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(sent: Int32, droppedSends: Int32) {
        self.sent = sent
        self.droppedSends = droppedSends
    }

    

    
}

#if compiler(>=6)
extension ChannelDemoOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeChannelDemoOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ChannelDemoOutput {
        return
            try ChannelDemoOutput(
                sent: FfiConverterInt32.read(from: &buf), 
                droppedSends: FfiConverterInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ChannelDemoOutput, into buf: inout [UInt8]) {
        FfiConverterInt32.write(value.sent, into: &buf)
        FfiConverterInt32.write(value.droppedSends, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoOutput_lift(_ buf: RustBuffer) throws -> ChannelDemoOutput {
    return try FfiConverterTypeChannelDemoOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeChannelDemoOutput_lower(_ value: ChannelDemoOutput) -> RustBuffer {
    return FfiConverterTypeChannelDemoOutput.lower(value)
}


/**
 * `ClampInput` 미러 — 실제 타입 `crate::ClampInput`.
 */
public struct ClampInput: Equatable, Hashable {
    public var max: Double
    public var min: Double
    public var value: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(max: Double, min: Double, value: Double) {
        self.max = max
        self.min = min
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension ClampInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeClampInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ClampInput {
        return
            try ClampInput(
                max: FfiConverterDouble.read(from: &buf), 
                min: FfiConverterDouble.read(from: &buf), 
                value: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: ClampInput, into buf: inout [UInt8]) {
        FfiConverterDouble.write(value.max, into: &buf)
        FfiConverterDouble.write(value.min, into: &buf)
        FfiConverterDouble.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeClampInput_lift(_ buf: RustBuffer) throws -> ClampInput {
    return try FfiConverterTypeClampInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeClampInput_lower(_ value: ClampInput) -> RustBuffer {
    return FfiConverterTypeClampInput.lower(value)
}


/**
 * `ClampOutput` 미러 — 실제 타입 `crate::ClampOutput`.
 */
public struct ClampOutput: Equatable, Hashable {
    public var value: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: Double) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension ClampOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeClampOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ClampOutput {
        return
            try ClampOutput(
                value: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: ClampOutput, into buf: inout [UInt8]) {
        FfiConverterDouble.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeClampOutput_lift(_ buf: RustBuffer) throws -> ClampOutput {
    return try FfiConverterTypeClampOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeClampOutput_lower(_ value: ClampOutput) -> RustBuffer {
    return FfiConverterTypeClampOutput.lower(value)
}


/**
 * `CreateItemInput` 미러 — 실제 타입 `crate::CreateItemInput`.
 */
public struct CreateItemInput: Equatable, Hashable {
    public var name: String
    public var value: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(name: String, value: Int64) {
        self.name = name
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension CreateItemInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCreateItemInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CreateItemInput {
        return
            try CreateItemInput(
                name: FfiConverterString.read(from: &buf), 
                value: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: CreateItemInput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterInt64.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCreateItemInput_lift(_ buf: RustBuffer) throws -> CreateItemInput {
    return try FfiConverterTypeCreateItemInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCreateItemInput_lower(_ value: CreateItemInput) -> RustBuffer {
    return FfiConverterTypeCreateItemInput.lower(value)
}


/**
 * `CreateItemOutput` 미러 — 실제 타입 `crate::CreateItemOutput`.
 */
public struct CreateItemOutput: Equatable, Hashable {
    public var item: Item

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(item: Item) {
        self.item = item
    }

    

    
}

#if compiler(>=6)
extension CreateItemOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeCreateItemOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> CreateItemOutput {
        return
            try CreateItemOutput(
                item: FfiConverterTypeItem.read(from: &buf)
        )
    }

    public static func write(_ value: CreateItemOutput, into buf: inout [UInt8]) {
        FfiConverterTypeItem.write(value.item, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCreateItemOutput_lift(_ buf: RustBuffer) throws -> CreateItemOutput {
    return try FfiConverterTypeCreateItemOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeCreateItemOutput_lower(_ value: CreateItemOutput) -> RustBuffer {
    return FfiConverterTypeCreateItemOutput.lower(value)
}


/**
 * `DeviceDemoOutput` 미러 — 실제 타입 `crate::DeviceDemoOutput`.
 */
public struct DeviceDemoOutput: Equatable, Hashable {
    public var os: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(os: String) {
        self.os = os
    }

    

    
}

#if compiler(>=6)
extension DeviceDemoOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDeviceDemoOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DeviceDemoOutput {
        return
            try DeviceDemoOutput(
                os: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: DeviceDemoOutput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.os, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDeviceDemoOutput_lift(_ buf: RustBuffer) throws -> DeviceDemoOutput {
    return try FfiConverterTypeDeviceDemoOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDeviceDemoOutput_lower(_ value: DeviceDemoOutput) -> RustBuffer {
    return FfiConverterTypeDeviceDemoOutput.lower(value)
}


/**
 * `DivideInput` 미러 — 실제 타입 `crate::DivideInput`.
 */
public struct DivideInput: Equatable, Hashable {
    public var a: Int64
    public var b: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(a: Int64, b: Int64) {
        self.a = a
        self.b = b
    }

    

    
}

#if compiler(>=6)
extension DivideInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDivideInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DivideInput {
        return
            try DivideInput(
                a: FfiConverterInt64.read(from: &buf), 
                b: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: DivideInput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.a, into: &buf)
        FfiConverterInt64.write(value.b, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDivideInput_lift(_ buf: RustBuffer) throws -> DivideInput {
    return try FfiConverterTypeDivideInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDivideInput_lower(_ value: DivideInput) -> RustBuffer {
    return FfiConverterTypeDivideInput.lower(value)
}


/**
 * `DivideOutput` 미러 — 실제 타입 `crate::DivideOutput`.
 */
public struct DivideOutput: Equatable, Hashable {
    public var value: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: Int64) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension DivideOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeDivideOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> DivideOutput {
        return
            try DivideOutput(
                value: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: DivideOutput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDivideOutput_lift(_ buf: RustBuffer) throws -> DivideOutput {
    return try FfiConverterTypeDivideOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeDivideOutput_lower(_ value: DivideOutput) -> RustBuffer {
    return FfiConverterTypeDivideOutput.lower(value)
}


/**
 * `EchoGroupsInput` 미러 — 실제 타입 `crate::EchoGroupsInput`.
 */
public struct EchoGroupsInput: Equatable, Hashable {
    public var groups: [String: [String]]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(groups: [String: [String]]) {
        self.groups = groups
    }

    

    
}

#if compiler(>=6)
extension EchoGroupsInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEchoGroupsInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EchoGroupsInput {
        return
            try EchoGroupsInput(
                groups: FfiConverterDictionaryStringSequenceString.read(from: &buf)
        )
    }

    public static func write(_ value: EchoGroupsInput, into buf: inout [UInt8]) {
        FfiConverterDictionaryStringSequenceString.write(value.groups, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEchoGroupsInput_lift(_ buf: RustBuffer) throws -> EchoGroupsInput {
    return try FfiConverterTypeEchoGroupsInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEchoGroupsInput_lower(_ value: EchoGroupsInput) -> RustBuffer {
    return FfiConverterTypeEchoGroupsInput.lower(value)
}


/**
 * `EchoGroupsOutput` 미러 — 실제 타입 `crate::EchoGroupsOutput`.
 */
public struct EchoGroupsOutput: Equatable, Hashable {
    public var groups: [String: [String]]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(groups: [String: [String]]) {
        self.groups = groups
    }

    

    
}

#if compiler(>=6)
extension EchoGroupsOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEchoGroupsOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EchoGroupsOutput {
        return
            try EchoGroupsOutput(
                groups: FfiConverterDictionaryStringSequenceString.read(from: &buf)
        )
    }

    public static func write(_ value: EchoGroupsOutput, into buf: inout [UInt8]) {
        FfiConverterDictionaryStringSequenceString.write(value.groups, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEchoGroupsOutput_lift(_ buf: RustBuffer) throws -> EchoGroupsOutput {
    return try FfiConverterTypeEchoGroupsOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEchoGroupsOutput_lower(_ value: EchoGroupsOutput) -> RustBuffer {
    return FfiConverterTypeEchoGroupsOutput.lower(value)
}


/**
 * `EmitDemoInput` 미러 — 실제 타입 `crate::EmitDemoInput`.
 */
public struct EmitDemoInput: Equatable, Hashable {
    public var ticks: Int64
    public var stepDelayMs: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(ticks: Int64, stepDelayMs: Int64) {
        self.ticks = ticks
        self.stepDelayMs = stepDelayMs
    }

    

    
}

#if compiler(>=6)
extension EmitDemoInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEmitDemoInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EmitDemoInput {
        return
            try EmitDemoInput(
                ticks: FfiConverterInt64.read(from: &buf), 
                stepDelayMs: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: EmitDemoInput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.ticks, into: &buf)
        FfiConverterInt64.write(value.stepDelayMs, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEmitDemoInput_lift(_ buf: RustBuffer) throws -> EmitDemoInput {
    return try FfiConverterTypeEmitDemoInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEmitDemoInput_lower(_ value: EmitDemoInput) -> RustBuffer {
    return FfiConverterTypeEmitDemoInput.lower(value)
}


/**
 * `EmitDemoOutput` 미러 — 실제 타입 `crate::EmitDemoOutput`.
 */
public struct EmitDemoOutput: Equatable, Hashable {
    public var emitted: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(emitted: Int64) {
        self.emitted = emitted
    }

    

    
}

#if compiler(>=6)
extension EmitDemoOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeEmitDemoOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> EmitDemoOutput {
        return
            try EmitDemoOutput(
                emitted: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: EmitDemoOutput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.emitted, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEmitDemoOutput_lift(_ buf: RustBuffer) throws -> EmitDemoOutput {
    return try FfiConverterTypeEmitDemoOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeEmitDemoOutput_lower(_ value: EmitDemoOutput) -> RustBuffer {
    return FfiConverterTypeEmitDemoOutput.lower(value)
}


/**
 * `GaugeInput` 미러 — 실제 타입 `crate::GaugeInput`.
 */
public struct GaugeInput: Equatable, Hashable {
    public var limit: UInt64
    public var offset: UInt32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(limit: UInt64, offset: UInt32) {
        self.limit = limit
        self.offset = offset
    }

    

    
}

#if compiler(>=6)
extension GaugeInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGaugeInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GaugeInput {
        return
            try GaugeInput(
                limit: FfiConverterUInt64.read(from: &buf), 
                offset: FfiConverterUInt32.read(from: &buf)
        )
    }

    public static func write(_ value: GaugeInput, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.limit, into: &buf)
        FfiConverterUInt32.write(value.offset, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGaugeInput_lift(_ buf: RustBuffer) throws -> GaugeInput {
    return try FfiConverterTypeGaugeInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGaugeInput_lower(_ value: GaugeInput) -> RustBuffer {
    return FfiConverterTypeGaugeInput.lower(value)
}


/**
 * `GaugeOutput` 미러 — 실제 타입 `crate::GaugeOutput`.
 */
public struct GaugeOutput: Equatable, Hashable {
    public var next: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(next: UInt64) {
        self.next = next
    }

    

    
}

#if compiler(>=6)
extension GaugeOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGaugeOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GaugeOutput {
        return
            try GaugeOutput(
                next: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: GaugeOutput, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.next, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGaugeOutput_lift(_ buf: RustBuffer) throws -> GaugeOutput {
    return try FfiConverterTypeGaugeOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGaugeOutput_lower(_ value: GaugeOutput) -> RustBuffer {
    return FfiConverterTypeGaugeOutput.lower(value)
}


/**
 * `GreetInput` 미러 — 실제 타입 `crate::GreetInput`.
 */
public struct GreetInput: Equatable, Hashable {
    public var name: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(name: String) {
        self.name = name
    }

    

    
}

#if compiler(>=6)
extension GreetInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGreetInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GreetInput {
        return
            try GreetInput(
                name: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: GreetInput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.name, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGreetInput_lift(_ buf: RustBuffer) throws -> GreetInput {
    return try FfiConverterTypeGreetInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGreetInput_lower(_ value: GreetInput) -> RustBuffer {
    return FfiConverterTypeGreetInput.lower(value)
}


/**
 * `GreetOutput` 미러 — 실제 타입 `crate::GreetOutput`.
 */
public struct GreetOutput: Equatable, Hashable {
    public var message: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(message: String) {
        self.message = message
    }

    

    
}

#if compiler(>=6)
extension GreetOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeGreetOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> GreetOutput {
        return
            try GreetOutput(
                message: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: GreetOutput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.message, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGreetOutput_lift(_ buf: RustBuffer) throws -> GreetOutput {
    return try FfiConverterTypeGreetOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeGreetOutput_lower(_ value: GreetOutput) -> RustBuffer {
    return FfiConverterTypeGreetOutput.lower(value)
}


/**
 * `IsEvenInput` 미러 — 실제 타입 `crate::IsEvenInput`.
 */
public struct IsEvenInput: Equatable, Hashable {
    public var n: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(n: Int64) {
        self.n = n
    }

    

    
}

#if compiler(>=6)
extension IsEvenInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIsEvenInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IsEvenInput {
        return
            try IsEvenInput(
                n: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: IsEvenInput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.n, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIsEvenInput_lift(_ buf: RustBuffer) throws -> IsEvenInput {
    return try FfiConverterTypeIsEvenInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIsEvenInput_lower(_ value: IsEvenInput) -> RustBuffer {
    return FfiConverterTypeIsEvenInput.lower(value)
}


/**
 * `IsEvenOutput` 미러 — 실제 타입 `crate::IsEvenOutput`.
 */
public struct IsEvenOutput: Equatable, Hashable {
    public var result: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(result: Bool) {
        self.result = result
    }

    

    
}

#if compiler(>=6)
extension IsEvenOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeIsEvenOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> IsEvenOutput {
        return
            try IsEvenOutput(
                result: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: IsEvenOutput, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.result, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIsEvenOutput_lift(_ buf: RustBuffer) throws -> IsEvenOutput {
    return try FfiConverterTypeIsEvenOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeIsEvenOutput_lower(_ value: IsEvenOutput) -> RustBuffer {
    return FfiConverterTypeIsEvenOutput.lower(value)
}


/**
 * `Item` 미러 — 실제 타입 `crate::Item`.
 */
public struct Item: Equatable, Hashable {
    public var active: Bool
    public var name: String
    public var value: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(active: Bool, name: String, value: Int64) {
        self.active = active
        self.name = name
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension Item: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeItem: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> Item {
        return
            try Item(
                active: FfiConverterBool.read(from: &buf), 
                name: FfiConverterString.read(from: &buf), 
                value: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: Item, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.active, into: &buf)
        FfiConverterString.write(value.name, into: &buf)
        FfiConverterInt64.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeItem_lift(_ buf: RustBuffer) throws -> Item {
    return try FfiConverterTypeItem.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeItem_lower(_ value: Item) -> RustBuffer {
    return FfiConverterTypeItem.lower(value)
}


/**
 * `KindEchoInput` 미러 — 실제 타입 `crate::KindEchoInput`.
 */
public struct KindEchoInput: Equatable, Hashable {
    public var kind: OpKind

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(kind: OpKind) {
        self.kind = kind
    }

    

    
}

#if compiler(>=6)
extension KindEchoInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeKindEchoInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> KindEchoInput {
        return
            try KindEchoInput(
                kind: FfiConverterTypeOpKind.read(from: &buf)
        )
    }

    public static func write(_ value: KindEchoInput, into buf: inout [UInt8]) {
        FfiConverterTypeOpKind.write(value.kind, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeKindEchoInput_lift(_ buf: RustBuffer) throws -> KindEchoInput {
    return try FfiConverterTypeKindEchoInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeKindEchoInput_lower(_ value: KindEchoInput) -> RustBuffer {
    return FfiConverterTypeKindEchoInput.lower(value)
}


/**
 * `KindEchoOutput` 미러 — 실제 타입 `crate::KindEchoOutput`.
 */
public struct KindEchoOutput: Equatable, Hashable {
    public var echoed: OpKind

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(echoed: OpKind) {
        self.echoed = echoed
    }

    

    
}

#if compiler(>=6)
extension KindEchoOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeKindEchoOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> KindEchoOutput {
        return
            try KindEchoOutput(
                echoed: FfiConverterTypeOpKind.read(from: &buf)
        )
    }

    public static func write(_ value: KindEchoOutput, into buf: inout [UInt8]) {
        FfiConverterTypeOpKind.write(value.echoed, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeKindEchoOutput_lift(_ buf: RustBuffer) throws -> KindEchoOutput {
    return try FfiConverterTypeKindEchoOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeKindEchoOutput_lower(_ value: KindEchoOutput) -> RustBuffer {
    return FfiConverterTypeKindEchoOutput.lower(value)
}


/**
 * `MultiplyInput` 미러 — 실제 타입 `crate::MultiplyInput`.
 */
public struct MultiplyInput: Equatable, Hashable {
    public var a: Double
    public var b: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(a: Double, b: Double) {
        self.a = a
        self.b = b
    }

    

    
}

#if compiler(>=6)
extension MultiplyInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultiplyInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultiplyInput {
        return
            try MultiplyInput(
                a: FfiConverterDouble.read(from: &buf), 
                b: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: MultiplyInput, into buf: inout [UInt8]) {
        FfiConverterDouble.write(value.a, into: &buf)
        FfiConverterDouble.write(value.b, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultiplyInput_lift(_ buf: RustBuffer) throws -> MultiplyInput {
    return try FfiConverterTypeMultiplyInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultiplyInput_lower(_ value: MultiplyInput) -> RustBuffer {
    return FfiConverterTypeMultiplyInput.lower(value)
}


/**
 * `MultiplyOutput` 미러 — 실제 타입 `crate::MultiplyOutput`.
 */
public struct MultiplyOutput: Equatable, Hashable {
    public var value: Double

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: Double) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension MultiplyOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeMultiplyOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> MultiplyOutput {
        return
            try MultiplyOutput(
                value: FfiConverterDouble.read(from: &buf)
        )
    }

    public static func write(_ value: MultiplyOutput, into buf: inout [UInt8]) {
        FfiConverterDouble.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultiplyOutput_lift(_ buf: RustBuffer) throws -> MultiplyOutput {
    return try FfiConverterTypeMultiplyOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeMultiplyOutput_lower(_ value: MultiplyOutput) -> RustBuffer {
    return FfiConverterTypeMultiplyOutput.lower(value)
}


/**
 * `PlatformNativeInfoOutput` 미러 — 실제 타입 `crate::PlatformNativeInfoOutput`.
 */
public struct PlatformNativeInfoOutput: Equatable, Hashable {
    public var os: String
    public var windowKind: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(os: String, windowKind: String) {
        self.os = os
        self.windowKind = windowKind
    }

    

    
}

#if compiler(>=6)
extension PlatformNativeInfoOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypePlatformNativeInfoOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> PlatformNativeInfoOutput {
        return
            try PlatformNativeInfoOutput(
                os: FfiConverterString.read(from: &buf), 
                windowKind: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: PlatformNativeInfoOutput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.os, into: &buf)
        FfiConverterString.write(value.windowKind, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePlatformNativeInfoOutput_lift(_ buf: RustBuffer) throws -> PlatformNativeInfoOutput {
    return try FfiConverterTypePlatformNativeInfoOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypePlatformNativeInfoOutput_lower(_ value: PlatformNativeInfoOutput) -> RustBuffer {
    return FfiConverterTypePlatformNativeInfoOutput.lower(value)
}


/**
 * `ProcessItemInput` 미러 — 실제 타입 `crate::ProcessItemInput`.
 */
public struct ProcessItemInput: Equatable, Hashable {
    public var item: Item

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(item: Item) {
        self.item = item
    }

    

    
}

#if compiler(>=6)
extension ProcessItemInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeProcessItemInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ProcessItemInput {
        return
            try ProcessItemInput(
                item: FfiConverterTypeItem.read(from: &buf)
        )
    }

    public static func write(_ value: ProcessItemInput, into buf: inout [UInt8]) {
        FfiConverterTypeItem.write(value.item, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProcessItemInput_lift(_ buf: RustBuffer) throws -> ProcessItemInput {
    return try FfiConverterTypeProcessItemInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProcessItemInput_lower(_ value: ProcessItemInput) -> RustBuffer {
    return FfiConverterTypeProcessItemInput.lower(value)
}


/**
 * `ProcessItemOutput` 미러 — 실제 타입 `crate::ProcessItemOutput`.
 */
public struct ProcessItemOutput: Equatable, Hashable {
    public var doubled: Bool
    public var item: Item

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(doubled: Bool, item: Item) {
        self.doubled = doubled
        self.item = item
    }

    

    
}

#if compiler(>=6)
extension ProcessItemOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeProcessItemOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ProcessItemOutput {
        return
            try ProcessItemOutput(
                doubled: FfiConverterBool.read(from: &buf), 
                item: FfiConverterTypeItem.read(from: &buf)
        )
    }

    public static func write(_ value: ProcessItemOutput, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.doubled, into: &buf)
        FfiConverterTypeItem.write(value.item, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProcessItemOutput_lift(_ buf: RustBuffer) throws -> ProcessItemOutput {
    return try FfiConverterTypeProcessItemOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeProcessItemOutput_lower(_ value: ProcessItemOutput) -> RustBuffer {
    return FfiConverterTypeProcessItemOutput.lower(value)
}


/**
 * `RegistryDemoInput` 미러 — 실제 타입 `crate::RegistryDemoInput`.
 */
public struct RegistryDemoInput: Equatable, Hashable {
    public var op: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(op: String) {
        self.op = op
    }

    

    
}

#if compiler(>=6)
extension RegistryDemoInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeRegistryDemoInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> RegistryDemoInput {
        return
            try RegistryDemoInput(
                op: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: RegistryDemoInput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.op, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRegistryDemoInput_lift(_ buf: RustBuffer) throws -> RegistryDemoInput {
    return try FfiConverterTypeRegistryDemoInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRegistryDemoInput_lower(_ value: RegistryDemoInput) -> RustBuffer {
    return FfiConverterTypeRegistryDemoInput.lower(value)
}


/**
 * `RegistryDemoOutput` 미러 — 실제 타입 `crate::RegistryDemoOutput`.
 */
public struct RegistryDemoOutput: Equatable, Hashable {
    public var ok: Bool
    public var frozen: Bool
    public var message: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(ok: Bool, frozen: Bool, message: String) {
        self.ok = ok
        self.frozen = frozen
        self.message = message
    }

    

    
}

#if compiler(>=6)
extension RegistryDemoOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeRegistryDemoOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> RegistryDemoOutput {
        return
            try RegistryDemoOutput(
                ok: FfiConverterBool.read(from: &buf), 
                frozen: FfiConverterBool.read(from: &buf), 
                message: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: RegistryDemoOutput, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.ok, into: &buf)
        FfiConverterBool.write(value.frozen, into: &buf)
        FfiConverterString.write(value.message, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRegistryDemoOutput_lift(_ buf: RustBuffer) throws -> RegistryDemoOutput {
    return try FfiConverterTypeRegistryDemoOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRegistryDemoOutput_lower(_ value: RegistryDemoOutput) -> RustBuffer {
    return FfiConverterTypeRegistryDemoOutput.lower(value)
}


/**
 * `ResourceCloseInput` 미러 — 실제 타입 `crate::ResourceCloseInput`.
 */
public struct ResourceCloseInput: Equatable, Hashable {
    public var handle: UInt32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(handle: UInt32) {
        self.handle = handle
    }

    

    
}

#if compiler(>=6)
extension ResourceCloseInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceCloseInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceCloseInput {
        return
            try ResourceCloseInput(
                handle: FfiConverterUInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceCloseInput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.handle, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceCloseInput_lift(_ buf: RustBuffer) throws -> ResourceCloseInput {
    return try FfiConverterTypeResourceCloseInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceCloseInput_lower(_ value: ResourceCloseInput) -> RustBuffer {
    return FfiConverterTypeResourceCloseInput.lower(value)
}


/**
 * `ResourceCloseOutput` 미러 — 실제 타입 `crate::ResourceCloseOutput`.
 */
public struct ResourceCloseOutput: Equatable, Hashable {
    public var closed: Bool

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(closed: Bool) {
        self.closed = closed
    }

    

    
}

#if compiler(>=6)
extension ResourceCloseOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceCloseOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceCloseOutput {
        return
            try ResourceCloseOutput(
                closed: FfiConverterBool.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceCloseOutput, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.closed, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceCloseOutput_lift(_ buf: RustBuffer) throws -> ResourceCloseOutput {
    return try FfiConverterTypeResourceCloseOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceCloseOutput_lower(_ value: ResourceCloseOutput) -> RustBuffer {
    return FfiConverterTypeResourceCloseOutput.lower(value)
}


/**
 * `ResourceHandleOutput` 미러 — 실제 타입 `crate::ResourceHandleOutput`.
 */
public struct ResourceHandleOutput: Equatable, Hashable {
    public var handle: UInt32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(handle: UInt32) {
        self.handle = handle
    }

    

    
}

#if compiler(>=6)
extension ResourceHandleOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceHandleOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceHandleOutput {
        return
            try ResourceHandleOutput(
                handle: FfiConverterUInt32.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceHandleOutput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.handle, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceHandleOutput_lift(_ buf: RustBuffer) throws -> ResourceHandleOutput {
    return try FfiConverterTypeResourceHandleOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceHandleOutput_lower(_ value: ResourceHandleOutput) -> RustBuffer {
    return FfiConverterTypeResourceHandleOutput.lower(value)
}


/**
 * `ResourceOpenInput` 미러 — 실제 타입 `crate::ResourceOpenInput`.
 */
public struct ResourceOpenInput: Equatable, Hashable {
    public var initial: [String: String]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(initial: [String: String]) {
        self.initial = initial
    }

    

    
}

#if compiler(>=6)
extension ResourceOpenInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceOpenInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceOpenInput {
        return
            try ResourceOpenInput(
                initial: FfiConverterDictionaryStringString.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceOpenInput, into buf: inout [UInt8]) {
        FfiConverterDictionaryStringString.write(value.initial, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceOpenInput_lift(_ buf: RustBuffer) throws -> ResourceOpenInput {
    return try FfiConverterTypeResourceOpenInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceOpenInput_lower(_ value: ResourceOpenInput) -> RustBuffer {
    return FfiConverterTypeResourceOpenInput.lower(value)
}


/**
 * `ResourceReadInput` 미러 — 실제 타입 `crate::ResourceReadInput`.
 */
public struct ResourceReadInput: Equatable, Hashable {
    public var handle: UInt32
    public var key: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(handle: UInt32, key: String) {
        self.handle = handle
        self.key = key
    }

    

    
}

#if compiler(>=6)
extension ResourceReadInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceReadInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceReadInput {
        return
            try ResourceReadInput(
                handle: FfiConverterUInt32.read(from: &buf), 
                key: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceReadInput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.handle, into: &buf)
        FfiConverterString.write(value.key, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceReadInput_lift(_ buf: RustBuffer) throws -> ResourceReadInput {
    return try FfiConverterTypeResourceReadInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceReadInput_lower(_ value: ResourceReadInput) -> RustBuffer {
    return FfiConverterTypeResourceReadInput.lower(value)
}


/**
 * `ResourceReadOutput` 미러 — 실제 타입 `crate::ResourceReadOutput`.
 */
public struct ResourceReadOutput: Equatable, Hashable {
    public var found: Bool
    public var value: String?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(found: Bool, value: String?) {
        self.found = found
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension ResourceReadOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceReadOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceReadOutput {
        return
            try ResourceReadOutput(
                found: FfiConverterBool.read(from: &buf), 
                value: FfiConverterOptionString.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceReadOutput, into buf: inout [UInt8]) {
        FfiConverterBool.write(value.found, into: &buf)
        FfiConverterOptionString.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceReadOutput_lift(_ buf: RustBuffer) throws -> ResourceReadOutput {
    return try FfiConverterTypeResourceReadOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceReadOutput_lower(_ value: ResourceReadOutput) -> RustBuffer {
    return FfiConverterTypeResourceReadOutput.lower(value)
}


/**
 * `ResourceWriteInput` 미러 — 실제 타입 `crate::ResourceWriteInput`.
 */
public struct ResourceWriteInput: Equatable, Hashable {
    public var handle: UInt32
    public var key: String
    public var value: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(handle: UInt32, key: String, value: String) {
        self.handle = handle
        self.key = key
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension ResourceWriteInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceWriteInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceWriteInput {
        return
            try ResourceWriteInput(
                handle: FfiConverterUInt32.read(from: &buf), 
                key: FfiConverterString.read(from: &buf), 
                value: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceWriteInput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.handle, into: &buf)
        FfiConverterString.write(value.key, into: &buf)
        FfiConverterString.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceWriteInput_lift(_ buf: RustBuffer) throws -> ResourceWriteInput {
    return try FfiConverterTypeResourceWriteInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceWriteInput_lower(_ value: ResourceWriteInput) -> RustBuffer {
    return FfiConverterTypeResourceWriteInput.lower(value)
}


/**
 * `ResourceWriteOutput` 미러 — 실제 타입 `crate::ResourceWriteOutput`.
 */
public struct ResourceWriteOutput: Equatable, Hashable {
    public var entries: UInt64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(entries: UInt64) {
        self.entries = entries
    }

    

    
}

#if compiler(>=6)
extension ResourceWriteOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeResourceWriteOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ResourceWriteOutput {
        return
            try ResourceWriteOutput(
                entries: FfiConverterUInt64.read(from: &buf)
        )
    }

    public static func write(_ value: ResourceWriteOutput, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.entries, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceWriteOutput_lift(_ buf: RustBuffer) throws -> ResourceWriteOutput {
    return try FfiConverterTypeResourceWriteOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeResourceWriteOutput_lower(_ value: ResourceWriteOutput) -> RustBuffer {
    return FfiConverterTypeResourceWriteOutput.lower(value)
}


/**
 * `ScoreTotalInput` 미러 — 실제 타입 `crate::ScoreTotalInput`.
 */
public struct ScoreTotalInput: Equatable, Hashable {
    public var scores: [String: Int64]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(scores: [String: Int64]) {
        self.scores = scores
    }

    

    
}

#if compiler(>=6)
extension ScoreTotalInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeScoreTotalInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ScoreTotalInput {
        return
            try ScoreTotalInput(
                scores: FfiConverterDictionaryStringInt64.read(from: &buf)
        )
    }

    public static func write(_ value: ScoreTotalInput, into buf: inout [UInt8]) {
        FfiConverterDictionaryStringInt64.write(value.scores, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeScoreTotalInput_lift(_ buf: RustBuffer) throws -> ScoreTotalInput {
    return try FfiConverterTypeScoreTotalInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeScoreTotalInput_lower(_ value: ScoreTotalInput) -> RustBuffer {
    return FfiConverterTypeScoreTotalInput.lower(value)
}


/**
 * `ScoreTotalOutput` 미러 — 실제 타입 `crate::ScoreTotalOutput`.
 */
public struct ScoreTotalOutput: Equatable, Hashable {
    public var count: UInt32
    public var total: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(count: UInt32, total: Int64) {
        self.count = count
        self.total = total
    }

    

    
}

#if compiler(>=6)
extension ScoreTotalOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeScoreTotalOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ScoreTotalOutput {
        return
            try ScoreTotalOutput(
                count: FfiConverterUInt32.read(from: &buf), 
                total: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: ScoreTotalOutput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.count, into: &buf)
        FfiConverterInt64.write(value.total, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeScoreTotalOutput_lift(_ buf: RustBuffer) throws -> ScoreTotalOutput {
    return try FfiConverterTypeScoreTotalOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeScoreTotalOutput_lower(_ value: ScoreTotalOutput) -> RustBuffer {
    return FfiConverterTypeScoreTotalOutput.lower(value)
}


/**
 * `SecureComputeInput` 미러 — 실제 타입 `crate::SecureComputeInput`.
 */
public struct SecureComputeInput: Equatable, Hashable {
    public var a: Int64
    public var b: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(a: Int64, b: Int64) {
        self.a = a
        self.b = b
    }

    

    
}

#if compiler(>=6)
extension SecureComputeInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecureComputeInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SecureComputeInput {
        return
            try SecureComputeInput(
                a: FfiConverterInt64.read(from: &buf), 
                b: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SecureComputeInput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.a, into: &buf)
        FfiConverterInt64.write(value.b, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecureComputeInput_lift(_ buf: RustBuffer) throws -> SecureComputeInput {
    return try FfiConverterTypeSecureComputeInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecureComputeInput_lower(_ value: SecureComputeInput) -> RustBuffer {
    return FfiConverterTypeSecureComputeInput.lower(value)
}


/**
 * `SecureComputeOutput` 미러 — 실제 타입 `crate::SecureComputeOutput`.
 */
public struct SecureComputeOutput: Equatable, Hashable {
    public var value: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(value: Int64) {
        self.value = value
    }

    

    
}

#if compiler(>=6)
extension SecureComputeOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSecureComputeOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SecureComputeOutput {
        return
            try SecureComputeOutput(
                value: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SecureComputeOutput, into buf: inout [UInt8]) {
        FfiConverterInt64.write(value.value, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecureComputeOutput_lift(_ buf: RustBuffer) throws -> SecureComputeOutput {
    return try FfiConverterTypeSecureComputeOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSecureComputeOutput_lower(_ value: SecureComputeOutput) -> RustBuffer {
    return FfiConverterTypeSecureComputeOutput.lower(value)
}


/**
 * `SizeOfInput` 미러 — 실제 타입 `crate::SizeOfInput`.
 */
public struct SizeOfInput: Equatable, Hashable {
    public var data: Data

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(data: Data) {
        self.data = data
    }

    

    
}

#if compiler(>=6)
extension SizeOfInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSizeOfInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SizeOfInput {
        return
            try SizeOfInput(
                data: FfiConverterData.read(from: &buf)
        )
    }

    public static func write(_ value: SizeOfInput, into buf: inout [UInt8]) {
        FfiConverterData.write(value.data, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSizeOfInput_lift(_ buf: RustBuffer) throws -> SizeOfInput {
    return try FfiConverterTypeSizeOfInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSizeOfInput_lower(_ value: SizeOfInput) -> RustBuffer {
    return FfiConverterTypeSizeOfInput.lower(value)
}


/**
 * `SizeOfOutput` 미러 — 실제 타입 `crate::SizeOfOutput`.
 */
public struct SizeOfOutput: Equatable, Hashable {
    public var checksum: UInt32
    public var len: UInt32

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(checksum: UInt32, len: UInt32) {
        self.checksum = checksum
        self.len = len
    }

    

    
}

#if compiler(>=6)
extension SizeOfOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSizeOfOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SizeOfOutput {
        return
            try SizeOfOutput(
                checksum: FfiConverterUInt32.read(from: &buf), 
                len: FfiConverterUInt32.read(from: &buf)
        )
    }

    public static func write(_ value: SizeOfOutput, into buf: inout [UInt8]) {
        FfiConverterUInt32.write(value.checksum, into: &buf)
        FfiConverterUInt32.write(value.len, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSizeOfOutput_lift(_ buf: RustBuffer) throws -> SizeOfOutput {
    return try FfiConverterTypeSizeOfOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSizeOfOutput_lower(_ value: SizeOfOutput) -> RustBuffer {
    return FfiConverterTypeSizeOfOutput.lower(value)
}


/**
 * `SpanInput` 미러 — 실제 타입 `crate::SpanInput`.
 */
public struct SpanInput: Equatable, Hashable {
    public var pair: SpanInputPair

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(pair: SpanInputPair) {
        self.pair = pair
    }

    

    
}

#if compiler(>=6)
extension SpanInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSpanInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SpanInput {
        return
            try SpanInput(
                pair: FfiConverterTypeSpanInputPair.read(from: &buf)
        )
    }

    public static func write(_ value: SpanInput, into buf: inout [UInt8]) {
        FfiConverterTypeSpanInputPair.write(value.pair, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpanInput_lift(_ buf: RustBuffer) throws -> SpanInput {
    return try FfiConverterTypeSpanInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpanInput_lower(_ value: SpanInput) -> RustBuffer {
    return FfiConverterTypeSpanInput.lower(value)
}


/**
 * `SpanInputPair` 미러 — 실제 타입 `(String, i64)`.
 */
public struct SpanInputPair: Equatable, Hashable {
    public var v0: String
    public var v1: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(v0: String, v1: Int64) {
        self.v0 = v0
        self.v1 = v1
    }

    

    
}

#if compiler(>=6)
extension SpanInputPair: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSpanInputPair: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SpanInputPair {
        return
            try SpanInputPair(
                v0: FfiConverterString.read(from: &buf), 
                v1: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SpanInputPair, into buf: inout [UInt8]) {
        FfiConverterString.write(value.v0, into: &buf)
        FfiConverterInt64.write(value.v1, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpanInputPair_lift(_ buf: RustBuffer) throws -> SpanInputPair {
    return try FfiConverterTypeSpanInputPair.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpanInputPair_lower(_ value: SpanInputPair) -> RustBuffer {
    return FfiConverterTypeSpanInputPair.lower(value)
}


/**
 * `SpanOutput` 미러 — 실제 타입 `crate::SpanOutput`.
 */
public struct SpanOutput: Equatable, Hashable {
    public var first: String
    public var second: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(first: String, second: Int64) {
        self.first = first
        self.second = second
    }

    

    
}

#if compiler(>=6)
extension SpanOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSpanOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SpanOutput {
        return
            try SpanOutput(
                first: FfiConverterString.read(from: &buf), 
                second: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SpanOutput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.first, into: &buf)
        FfiConverterInt64.write(value.second, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpanOutput_lift(_ buf: RustBuffer) throws -> SpanOutput {
    return try FfiConverterTypeSpanOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSpanOutput_lower(_ value: SpanOutput) -> RustBuffer {
    return FfiConverterTypeSpanOutput.lower(value)
}


/**
 * `SumListInput` 미러 — 실제 타입 `crate::SumListInput`.
 */
public struct SumListInput: Equatable, Hashable {
    public var numbers: [Int64]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(numbers: [Int64]) {
        self.numbers = numbers
    }

    

    
}

#if compiler(>=6)
extension SumListInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSumListInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SumListInput {
        return
            try SumListInput(
                numbers: FfiConverterSequenceInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SumListInput, into buf: inout [UInt8]) {
        FfiConverterSequenceInt64.write(value.numbers, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSumListInput_lift(_ buf: RustBuffer) throws -> SumListInput {
    return try FfiConverterTypeSumListInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSumListInput_lower(_ value: SumListInput) -> RustBuffer {
    return FfiConverterTypeSumListInput.lower(value)
}


/**
 * `SumListOutput` 미러 — 실제 타입 `crate::SumListOutput`.
 */
public struct SumListOutput: Equatable, Hashable {
    public var count: Int32
    public var total: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(count: Int32, total: Int64) {
        self.count = count
        self.total = total
    }

    

    
}

#if compiler(>=6)
extension SumListOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeSumListOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SumListOutput {
        return
            try SumListOutput(
                count: FfiConverterInt32.read(from: &buf), 
                total: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: SumListOutput, into buf: inout [UInt8]) {
        FfiConverterInt32.write(value.count, into: &buf)
        FfiConverterInt64.write(value.total, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSumListOutput_lift(_ buf: RustBuffer) throws -> SumListOutput {
    return try FfiConverterTypeSumListOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeSumListOutput_lower(_ value: SumListOutput) -> RustBuffer {
    return FfiConverterTypeSumListOutput.lower(value)
}


/**
 * `TagSetInput` 미러 — 실제 타입 `crate::TagSetInput`.
 */
public struct TagSetInput: Equatable, Hashable {
    public var ids: [Int64]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(ids: [Int64]) {
        self.ids = ids
    }

    

    
}

#if compiler(>=6)
extension TagSetInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTagSetInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TagSetInput {
        return
            try TagSetInput(
                ids: FfiConverterSequenceInt64.read(from: &buf)
        )
    }

    public static func write(_ value: TagSetInput, into buf: inout [UInt8]) {
        FfiConverterSequenceInt64.write(value.ids, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTagSetInput_lift(_ buf: RustBuffer) throws -> TagSetInput {
    return try FfiConverterTypeTagSetInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTagSetInput_lower(_ value: TagSetInput) -> RustBuffer {
    return FfiConverterTypeTagSetInput.lower(value)
}


/**
 * `TagSetOutput` 미러 — 실제 타입 `crate::TagSetOutput`.
 */
public struct TagSetOutput: Equatable, Hashable {
    public var tags: [String]

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(tags: [String]) {
        self.tags = tags
    }

    

    
}

#if compiler(>=6)
extension TagSetOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeTagSetOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> TagSetOutput {
        return
            try TagSetOutput(
                tags: FfiConverterSequenceString.read(from: &buf)
        )
    }

    public static func write(_ value: TagSetOutput, into buf: inout [UInt8]) {
        FfiConverterSequenceString.write(value.tags, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTagSetOutput_lift(_ buf: RustBuffer) throws -> TagSetOutput {
    return try FfiConverterTypeTagSetOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeTagSetOutput_lower(_ value: TagSetOutput) -> RustBuffer {
    return FfiConverterTypeTagSetOutput.lower(value)
}


/**
 * `ToUpperInput` 미러 — 실제 타입 `crate::ToUpperInput`.
 */
public struct ToUpperInput: Equatable, Hashable {
    public var s: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(s: String) {
        self.s = s
    }

    

    
}

#if compiler(>=6)
extension ToUpperInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeToUpperInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ToUpperInput {
        return
            try ToUpperInput(
                s: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: ToUpperInput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.s, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeToUpperInput_lift(_ buf: RustBuffer) throws -> ToUpperInput {
    return try FfiConverterTypeToUpperInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeToUpperInput_lower(_ value: ToUpperInput) -> RustBuffer {
    return FfiConverterTypeToUpperInput.lower(value)
}


/**
 * `ToUpperOutput` 미러 — 실제 타입 `crate::ToUpperOutput`.
 */
public struct ToUpperOutput: Equatable, Hashable {
    public var result: String

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(result: String) {
        self.result = result
    }

    

    
}

#if compiler(>=6)
extension ToUpperOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeToUpperOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> ToUpperOutput {
        return
            try ToUpperOutput(
                result: FfiConverterString.read(from: &buf)
        )
    }

    public static func write(_ value: ToUpperOutput, into buf: inout [UInt8]) {
        FfiConverterString.write(value.result, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeToUpperOutput_lift(_ buf: RustBuffer) throws -> ToUpperOutput {
    return try FfiConverterTypeToUpperOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeToUpperOutput_lower(_ value: ToUpperOutput) -> RustBuffer {
    return FfiConverterTypeToUpperOutput.lower(value)
}


/**
 * `WideAggInput` 미러 — 실제 타입 `crate::WideAggInput`.
 */
public struct WideAggInput: Equatable, Hashable {
    public var samples: [UInt64]
    public var offset: Int64?

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(samples: [UInt64], offset: Int64?) {
        self.samples = samples
        self.offset = offset
    }

    

    
}

#if compiler(>=6)
extension WideAggInput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeWideAggInput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> WideAggInput {
        return
            try WideAggInput(
                samples: FfiConverterSequenceUInt64.read(from: &buf), 
                offset: FfiConverterOptionInt64.read(from: &buf)
        )
    }

    public static func write(_ value: WideAggInput, into buf: inout [UInt8]) {
        FfiConverterSequenceUInt64.write(value.samples, into: &buf)
        FfiConverterOptionInt64.write(value.offset, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWideAggInput_lift(_ buf: RustBuffer) throws -> WideAggInput {
    return try FfiConverterTypeWideAggInput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWideAggInput_lower(_ value: WideAggInput) -> RustBuffer {
    return FfiConverterTypeWideAggInput.lower(value)
}


/**
 * `WideAggOutput` 미러 — 실제 타입 `crate::WideAggOutput`.
 */
public struct WideAggOutput: Equatable, Hashable {
    public var max: UInt64
    public var adjusted: Int64

    // Default memberwise initializers are never public by default, so we
    // declare one manually.
    public init(max: UInt64, adjusted: Int64) {
        self.max = max
        self.adjusted = adjusted
    }

    

    
}

#if compiler(>=6)
extension WideAggOutput: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeWideAggOutput: FfiConverterRustBuffer {
    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> WideAggOutput {
        return
            try WideAggOutput(
                max: FfiConverterUInt64.read(from: &buf), 
                adjusted: FfiConverterInt64.read(from: &buf)
        )
    }

    public static func write(_ value: WideAggOutput, into buf: inout [UInt8]) {
        FfiConverterUInt64.write(value.max, into: &buf)
        FfiConverterInt64.write(value.adjusted, into: &buf)
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWideAggOutput_lift(_ buf: RustBuffer) throws -> WideAggOutput {
    return try FfiConverterTypeWideAggOutput.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeWideAggOutput_lower(_ value: WideAggOutput) -> RustBuffer {
    return FfiConverterTypeWideAggOutput.lower(value)
}


/**
 * `OpKind` 미러 — 실제 타입 `crate::OpKind`(외부 태그 oneOf 스키마).
 */

public enum OpKind: Equatable, Hashable {
    
    case clear
    case set(value: Int64
    )





}

#if compiler(>=6)
extension OpKind: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeOpKind: FfiConverterRustBuffer {
    typealias SwiftType = OpKind

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> OpKind {
        let variant: Int32 = try readInt(&buf)
        switch variant {
        
        case 1: return .clear
        
        case 2: return .set(value: try FfiConverterInt64.read(from: &buf)
        )
        
        default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: OpKind, into buf: inout [UInt8]) {
        switch value {
        
        
        case .clear:
            writeInt(&buf, Int32(1))
        
        
        case let .set(value):
            writeInt(&buf, Int32(2))
            FfiConverterInt64.write(value, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeOpKind_lift(_ buf: RustBuffer) throws -> OpKind {
    return try FfiConverterTypeOpKind.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeOpKind_lower(_ value: OpKind) -> RustBuffer {
    return FfiConverterTypeOpKind.lower(value)
}



public 
enum RustraCommandFailure: Swift.Error, Equatable, Hashable, Foundation.LocalizedError {

    
    
    case Failure(code: String, detail: String, retryable: Bool
    )

    

    

    
    public var errorDescription: String? {
        String(reflecting: self)
    }
    
}

#if compiler(>=6)
extension RustraCommandFailure: Sendable {}
#endif

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public struct FfiConverterTypeRustraCommandFailure: FfiConverterRustBuffer {
    typealias SwiftType = RustraCommandFailure

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> RustraCommandFailure {
        let variant: Int32 = try readInt(&buf)
        switch variant {

        

        
        case 1: return .Failure(
            code: try FfiConverterString.read(from: &buf), 
            detail: try FfiConverterString.read(from: &buf), 
            retryable: try FfiConverterBool.read(from: &buf)
            )

         default: throw UniffiInternalError.unexpectedEnumCase
        }
    }

    public static func write(_ value: RustraCommandFailure, into buf: inout [UInt8]) {
        switch value {

        

        
        
        case let .Failure(code,detail,retryable):
            writeInt(&buf, Int32(1))
            FfiConverterString.write(code, into: &buf)
            FfiConverterString.write(detail, into: &buf)
            FfiConverterBool.write(retryable, into: &buf)
            
        }
    }
}


#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRustraCommandFailure_lift(_ buf: RustBuffer) throws -> RustraCommandFailure {
    return try FfiConverterTypeRustraCommandFailure.lift(buf)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
public func FfiConverterTypeRustraCommandFailure_lower(_ value: RustraCommandFailure) -> RustBuffer {
    return FfiConverterTypeRustraCommandFailure.lower(value)
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionInt64: FfiConverterRustBuffer {
    typealias SwiftType = Int64?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterInt64.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterInt64.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterOptionString: FfiConverterRustBuffer {
    typealias SwiftType = String?

    public static func write(_ value: SwiftType, into buf: inout [UInt8]) {
        guard let value = value else {
            writeInt(&buf, Int8(0))
            return
        }
        writeInt(&buf, Int8(1))
        FfiConverterString.write(value, into: &buf)
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> SwiftType {
        switch try readInt(&buf) as Int8 {
        case 0: return nil
        case 1: return try FfiConverterString.read(from: &buf)
        default: throw UniffiInternalError.unexpectedOptionalTag
        }
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceUInt64: FfiConverterRustBuffer {
    typealias SwiftType = [UInt64]

    public static func write(_ value: [UInt64], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterUInt64.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [UInt64] {
        let len: Int32 = try readInt(&buf)
        var seq = [UInt64]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterUInt64.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceInt64: FfiConverterRustBuffer {
    typealias SwiftType = [Int64]

    public static func write(_ value: [Int64], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterInt64.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [Int64] {
        let len: Int32 = try readInt(&buf)
        var seq = [Int64]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterInt64.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterSequenceString: FfiConverterRustBuffer {
    typealias SwiftType = [String]

    public static func write(_ value: [String], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for item in value {
            FfiConverterString.write(item, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String] {
        let len: Int32 = try readInt(&buf)
        var seq = [String]()
        seq.reserveCapacity(Int(len))
        for _ in 0 ..< len {
            seq.append(try FfiConverterString.read(from: &buf))
        }
        return seq
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryStringInt64: FfiConverterRustBuffer {
    public static func write(_ value: [String: Int64], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterString.write(key, into: &buf)
            FfiConverterInt64.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String: Int64] {
        let len: Int32 = try readInt(&buf)
        var dict = [String: Int64]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterString.read(from: &buf)
            let value = try FfiConverterInt64.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryStringString: FfiConverterRustBuffer {
    public static func write(_ value: [String: String], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterString.write(key, into: &buf)
            FfiConverterString.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String: String] {
        let len: Int32 = try readInt(&buf)
        var dict = [String: String]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterString.read(from: &buf)
            let value = try FfiConverterString.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}

#if swift(>=5.8)
@_documentation(visibility: private)
#endif
fileprivate struct FfiConverterDictionaryStringSequenceString: FfiConverterRustBuffer {
    public static func write(_ value: [String: [String]], into buf: inout [UInt8]) {
        let len = Int32(value.count)
        writeInt(&buf, len)
        for (key, value) in value {
            FfiConverterString.write(key, into: &buf)
            FfiConverterSequenceString.write(value, into: &buf)
        }
    }

    public static func read(from buf: inout (data: Data, offset: Data.Index)) throws -> [String: [String]] {
        let len: Int32 = try readInt(&buf)
        var dict = [String: [String]]()
        dict.reserveCapacity(Int(len))
        for _ in 0..<len {
            let key = try FfiConverterString.read(from: &buf)
            let value = try FfiConverterSequenceString.read(from: &buf)
            dict[key] = value
        }
        return dict
    }
}
/**
 * `addNumbers` — `crate::add_numbers` 커맨드의 UniFFI 타입 래퍼.
 */
public func addNumbers(input: AddNumbersInput)throws  -> AddNumbersOutput  {
    return try  FfiConverterTypeAddNumbersOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_addnumbers(
        FfiConverterTypeAddNumbersInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `benchAdd` — `crate::bench_add` 커맨드의 UniFFI 타입 래퍼.
 */
public func benchAdd(input: BenchAddInput)throws  -> BenchAddOutput  {
    return try  FfiConverterTypeBenchAddOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_benchadd(
        FfiConverterTypeBenchAddInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `benchEchoBytes` — `crate::bench_echo_bytes` 커맨드의 UniFFI 타입 래퍼.
 */
public func benchEchoBytes(input: BenchBytesPayload)throws  -> BenchBytesPayload  {
    return try  FfiConverterTypeBenchBytesPayload_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_benchechobytes(
        FfiConverterTypeBenchBytesPayload_lower(input),uniffiCallStatus
    )
})
}
/**
 * `benchEchoPair` — `crate::bench_echo_pair` 커맨드의 UniFFI 타입 래퍼.
 */
public func benchEchoPair(input: BenchPairPayload)throws  -> BenchPairPayload  {
    return try  FfiConverterTypeBenchPairPayload_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_benchechopair(
        FfiConverterTypeBenchPairPayload_lower(input),uniffiCallStatus
    )
})
}
/**
 * `benchEchoString` — `crate::bench_echo_string` 커맨드의 UniFFI 타입 래퍼.
 */
public func benchEchoString(input: BenchStringPayload)throws  -> BenchStringPayload  {
    return try  FfiConverterTypeBenchStringPayload_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_benchechostring(
        FfiConverterTypeBenchStringPayload_lower(input),uniffiCallStatus
    )
})
}
/**
 * `channelDemo` — `crate::channel_demo` 커맨드의 UniFFI 타입 래퍼.
 */
public func channelDemo(input: ChannelDemoInput)throws  -> ChannelDemoOutput  {
    return try  FfiConverterTypeChannelDemoOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_channeldemo(
        FfiConverterTypeChannelDemoInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `channelDemoBytes` — `crate::channel_demo_bytes` 커맨드의 UniFFI 타입 래퍼.
 */
public func channelDemoBytes(input: ChannelDemoBytesInput)throws  -> ChannelDemoBytesOutput  {
    return try  FfiConverterTypeChannelDemoBytesOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_channeldemobytes(
        FfiConverterTypeChannelDemoBytesInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `clamp` — `crate::clamp` 커맨드의 UniFFI 타입 래퍼.
 */
public func clamp(input: ClampInput)throws  -> ClampOutput  {
    return try  FfiConverterTypeClampOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_clamp(
        FfiConverterTypeClampInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * 계약 해시 — FFI `rustra_ffi_contract_hash` 와 동일 단일 소스.
 * 같은 dylib 안의 심볼을 직접 호출해 로직 복제를 피한다.
 */
public func contractHash()throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_contracthash(uniffiCallStatus
    )
})
}
/**
 * `createItem` — `crate::create_item` 커맨드의 UniFFI 타입 래퍼.
 */
public func createItem(input: CreateItemInput)throws  -> CreateItemOutput  {
    return try  FfiConverterTypeCreateItemOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_createitem(
        FfiConverterTypeCreateItemInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `deviceDemo` — `crate::device_demo`(unit 입력)의 UniFFI 타입 래퍼.
 */
public func deviceDemo()throws  -> DeviceDemoOutput  {
    return try  FfiConverterTypeDeviceDemoOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_devicedemo(uniffiCallStatus
    )
})
}
/**
 * `divide` — `crate::divide` 커맨드의 UniFFI 타입 래퍼.
 */
public func divide(input: DivideInput)throws  -> DivideOutput  {
    return try  FfiConverterTypeDivideOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_divide(
        FfiConverterTypeDivideInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `echoGroups` — `crate::echo_groups` 커맨드의 UniFFI 타입 래퍼.
 */
public func echoGroups(input: EchoGroupsInput)throws  -> EchoGroupsOutput  {
    return try  FfiConverterTypeEchoGroupsOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_echogroups(
        FfiConverterTypeEchoGroupsInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `emitDemo` — `crate::emit_demo` 커맨드의 UniFFI 타입 래퍼.
 */
public func emitDemo(input: EmitDemoInput)throws  -> EmitDemoOutput  {
    return try  FfiConverterTypeEmitDemoOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_emitdemo(
        FfiConverterTypeEmitDemoInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `gauge` — `crate::gauge` 커맨드의 UniFFI 타입 래퍼.
 */
public func gauge(input: GaugeInput)throws  -> GaugeOutput  {
    return try  FfiConverterTypeGaugeOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_gauge(
        FfiConverterTypeGaugeInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * 라이브 스키마 JSON — `Package::live_schema`.
 */
public func getSchema() -> String  {
    return try!  FfiConverterString.lift(try! rustCall() {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_getschema(uniffiCallStatus
    )
})
}
/**
 * `greet` — `crate::greet` 커맨드의 UniFFI 타입 래퍼.
 */
public func greet(input: GreetInput)throws  -> GreetOutput  {
    return try  FfiConverterTypeGreetOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_greet(
        FfiConverterTypeGreetInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * 이름 기반 JSON 호출 — `Package::invoke_json` 의 문자열 경계 래퍼.
 */
public func invokeJson(command: String, argsJson: String)throws  -> String  {
    return try  FfiConverterString.lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_invokejson(
        FfiConverterString.lower(command),
        FfiConverterString.lower(argsJson),uniffiCallStatus
    )
})
}
/**
 * `isEven` — `crate::is_even` 커맨드의 UniFFI 타입 래퍼.
 */
public func isEven(input: IsEvenInput)throws  -> IsEvenOutput  {
    return try  FfiConverterTypeIsEvenOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_iseven(
        FfiConverterTypeIsEvenInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `kindEcho` — `crate::kind_echo` 커맨드의 UniFFI 타입 래퍼.
 */
public func kindEcho(input: KindEchoInput)throws  -> KindEchoOutput  {
    return try  FfiConverterTypeKindEchoOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_kindecho(
        FfiConverterTypeKindEchoInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `multiply` — `crate::multiply` 커맨드의 UniFFI 타입 래퍼.
 */
public func multiply(input: MultiplyInput)throws  -> MultiplyOutput  {
    return try  FfiConverterTypeMultiplyOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_multiply(
        FfiConverterTypeMultiplyInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `platformNativeInfo` — `crate::platform_native_info`(unit 입력)의 UniFFI 타입 래퍼.
 */
public func platformNativeInfo()throws  -> PlatformNativeInfoOutput  {
    return try  FfiConverterTypePlatformNativeInfoOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_platformnativeinfo(uniffiCallStatus
    )
})
}
/**
 * `processItem` — `crate::process_item` 커맨드의 UniFFI 타입 래퍼.
 */
public func processItem(input: ProcessItemInput)throws  -> ProcessItemOutput  {
    return try  FfiConverterTypeProcessItemOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_processitem(
        FfiConverterTypeProcessItemInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `resourceClose` — `crate::resource_close` 커맨드의 UniFFI 타입 래퍼.
 */
public func resourceClose(input: ResourceCloseInput)throws  -> ResourceCloseOutput  {
    return try  FfiConverterTypeResourceCloseOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_resourceclose(
        FfiConverterTypeResourceCloseInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `resourceOpen` — `crate::resource_open` 커맨드의 UniFFI 타입 래퍼.
 */
public func resourceOpen(input: ResourceOpenInput)throws  -> ResourceHandleOutput  {
    return try  FfiConverterTypeResourceHandleOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_resourceopen(
        FfiConverterTypeResourceOpenInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `resourceRead` — `crate::resource_read` 커맨드의 UniFFI 타입 래퍼.
 */
public func resourceRead(input: ResourceReadInput)throws  -> ResourceReadOutput  {
    return try  FfiConverterTypeResourceReadOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_resourceread(
        FfiConverterTypeResourceReadInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `resourceWrite` — `crate::resource_write` 커맨드의 UniFFI 타입 래퍼.
 */
public func resourceWrite(input: ResourceWriteInput)throws  -> ResourceWriteOutput  {
    return try  FfiConverterTypeResourceWriteOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_resourcewrite(
        FfiConverterTypeResourceWriteInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `rustraRegistryDemo` — `crate::rustra_registry_demo` 커맨드의 UniFFI 타입 래퍼.
 */
public func rustraRegistryDemo(input: RegistryDemoInput)throws  -> RegistryDemoOutput  {
    return try  FfiConverterTypeRegistryDemoOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_rustraregistrydemo(
        FfiConverterTypeRegistryDemoInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `scoreTotal` — `crate::score_total` 커맨드의 UniFFI 타입 래퍼.
 */
public func scoreTotal(input: ScoreTotalInput)throws  -> ScoreTotalOutput  {
    return try  FfiConverterTypeScoreTotalOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_scoretotal(
        FfiConverterTypeScoreTotalInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `secureCompute` — `crate::secure_compute` 커맨드의 UniFFI 타입 래퍼.
 */
public func secureCompute(input: SecureComputeInput)throws  -> SecureComputeOutput  {
    return try  FfiConverterTypeSecureComputeOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_securecompute(
        FfiConverterTypeSecureComputeInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `sizeOf` — `crate::size_of` 커맨드의 UniFFI 타입 래퍼.
 */
public func sizeOf(input: SizeOfInput)throws  -> SizeOfOutput  {
    return try  FfiConverterTypeSizeOfOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_sizeof(
        FfiConverterTypeSizeOfInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `span` — `crate::span` 커맨드의 UniFFI 타입 래퍼.
 */
public func span(input: SpanInput)throws  -> SpanOutput  {
    return try  FfiConverterTypeSpanOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_span(
        FfiConverterTypeSpanInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `sumList` — `crate::sum_list` 커맨드의 UniFFI 타입 래퍼.
 */
public func sumList(input: SumListInput)throws  -> SumListOutput  {
    return try  FfiConverterTypeSumListOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_sumlist(
        FfiConverterTypeSumListInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `tagSet` — `crate::tag_set` 커맨드의 UniFFI 타입 래퍼.
 */
public func tagSet(input: TagSetInput)throws  -> TagSetOutput  {
    return try  FfiConverterTypeTagSetOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_tagset(
        FfiConverterTypeTagSetInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `toUpper` — `crate::to_upper` 커맨드의 UniFFI 타입 래퍼.
 */
public func toUpper(input: ToUpperInput)throws  -> ToUpperOutput  {
    return try  FfiConverterTypeToUpperOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_toupper(
        FfiConverterTypeToUpperInput_lower(input),uniffiCallStatus
    )
})
}
/**
 * `wideAgg` — `crate::wide_agg` 커맨드의 UniFFI 타입 래퍼.
 */
public func wideAgg(input: WideAggInput)throws  -> WideAggOutput  {
    return try  FfiConverterTypeWideAggOutput_lift(try rustCallWithError(FfiConverterTypeRustraCommandFailure_lift) {
        uniffiCallStatus in
    uniffi_rustra_calculator_example_fn_func_wideagg(
        FfiConverterTypeWideAggInput_lower(input),uniffiCallStatus
    )
})
}

private enum InitializationResult {
    case ok
    case contractVersionMismatch
    case apiChecksumMismatch
}
// Use a global variable to perform the versioning checks. Swift ensures that
// the code inside is only computed once.
private let initializationResult: InitializationResult = {
    // Get the bindings contract version from our ComponentInterface
    let bindings_contract_version = 30
    // Get the scaffolding contract version by calling the into the dylib
    let scaffolding_contract_version = ffi_rustra_calculator_example_uniffi_contract_version()
    if bindings_contract_version != scaffolding_contract_version {
        return InitializationResult.contractVersionMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_addnumbers() != 26346) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_benchadd() != 38735) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_benchechobytes() != 52368) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_benchechopair() != 16278) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_benchechostring() != 16098) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_channeldemo() != 19525) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_channeldemobytes() != 19944) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_clamp() != 53189) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_contracthash() != 63409) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_createitem() != 40725) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_devicedemo() != 48297) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_divide() != 33615) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_echogroups() != 19097) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_emitdemo() != 10571) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_gauge() != 43678) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_getschema() != 25805) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_greet() != 40983) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_invokejson() != 15830) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_iseven() != 52133) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_kindecho() != 18248) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_multiply() != 37534) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_platformnativeinfo() != 56716) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_processitem() != 6741) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_resourceclose() != 35746) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_resourceopen() != 12002) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_resourceread() != 50323) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_resourcewrite() != 63142) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_rustraregistrydemo() != 30147) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_scoretotal() != 64792) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_securecompute() != 7333) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_sizeof() != 10734) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_span() != 24023) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_sumlist() != 57074) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_tagset() != 3367) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_toupper() != 48777) {
        return InitializationResult.apiChecksumMismatch
    }
    if (uniffi_rustra_calculator_example_checksum_func_wideagg() != 55913) {
        return InitializationResult.apiChecksumMismatch
    }

    return InitializationResult.ok
}()

// Make the ensure init function public so that other modules which have external type references to
// our types can call it.
public func uniffiEnsureRustraCalculatorExampleInitialized() {
    switch initializationResult {
    case .ok:
        break
    case .contractVersionMismatch:
        fatalError("UniFFI contract version mismatch: try cleaning and rebuilding your project")
    case .apiChecksumMismatch:
        fatalError("UniFFI API checksum mismatch: try cleaning and rebuilding your project")
    }
}

// swiftlint:enable all