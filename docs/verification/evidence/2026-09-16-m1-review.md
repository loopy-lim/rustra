# M1 candidate independent final review

Review target: `/Users/loopy/dev/ll3/rustra-bridge/.worktrees/m1-registry-onboarding`, uncommitted candidate relative to `1f277de2e68e2242b7a6503e6e0ab731833e60e3`.

**Result: no remaining actionable correctness or integration findings; no review blockers.** The four verdicts below concern the scoped local candidate, not external G1/G2 acceptance or publication.

## Four verdicts

| Area                        | Verdict  | Basis                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Task 1 specification        | **PASS** | Exact public install selection, frozen npm install, version/source checks, canonical Cargo root identity, original scaffold before extension, actual generated Node stdio/Bun FFI calls, returned-field mutation, Rust patch upgrade/rollback, failure receipts, source/lock/generated/native hashes, and explicit exclusions are implemented. The final public v6 receipt proves the scoped cycle. |
| Task 1 quality              | **PASS** | Runner, fixture and provenance responsibilities are separated; diagnostics survive failures; the empty-error fail-open was fixed and independently verified. Final fixture replacement matches the complete builder expression and produces valid Rust syntax.                                                                                                                                      |
| Whole-branch integration    | **PASS** | Offline CI and read-only manual workflow wiring are coherent. Final EN/KO instructions, roadmap status, plan, receipt and attempt ledger distinguish public-package evidence, local CLI fixes, remote workflows, unpublished changes and remaining external acceptance.                                                                                                                             |
| CLI product fix correctness | **PASS** | Compatible exact stable versions inside configured carets are preserved; incompatible/unbounded inputs are rejected. Init/codegen use independent adapter metadata and version/coherence tools maintain it. Optional `InitHosts.nodeRange` preserves existing calls; the sole API snapshot change and CLI patch changeset disclose this additive option.                                            |

## Finding resolved during review

**P2 — empty runner exceptions could pass onboarding.** The earlier `runStep` catch returned `''` for `throw new Error()`, and the caller tested failure by truthiness. An independent temporary fixture reproduced `ok: true` with all later steps executed after initialization threw. Final code uses a nonempty diagnostic fallback across runner/patch/mutate catches and explicit `failure !== null` guards. Tests cover both an empty Error and thrown empty string; the final onboarding suite independently passed 20/20. This finding is closed.

The parent's live attempts also exposed missing FFI registration and an invalid partial-expression fixture rewrite. Final fixture construction includes public `Package::register_ffi()`, replaces the complete original builder expression, and rejects a missing anchor. I independently rendered a fresh scaffold, applied the final fixture helper and parsed the resulting Rust with `rustfmt --edition 2021 --emit stdout`: status 0. Final v6 subsequently completed actual host execution. No open fixture finding remains.

## Independent verification

- `node --test scripts/registry-consumer-gate.test.mjs`: **19/19 pass** at initial source-review point; the changed final fixture was separately inspected and syntax-checked after its stronger regression was added.
- `node --test packages/cli/dist-test/dependencies.test.js`: **10/10 pass**. These are parent-built compiled artifacts, with corresponding source inspected.
- `node --experimental-strip-types --test scripts/version-packages.test.ts scripts/check-release-coherence.test.ts`: **10/10 pass**.
- `node --experimental-strip-types --test scripts/onboarding-gate.test.ts`: **20/20 pass** after the final fail-closed correction.
- Final `git diff --check`: pass.
- API snapshot inspected: its sole diff is optional `InitHosts.nodeRange`.
- Final repository receipt is byte-for-byte identical to `/tmp/rustra-m1-live-20260916-v6/receipt.json`.

## Independent examination of the parent's live evidence

I did not rerun the network/native gate. I examined its final v6 receipt and retained files:

- `ok=true`, **88 steps**, no failed steps, **19.767 seconds**.
- All six executable runner/helper/version source hashes match both current worktree files and retained source copies.
- All **39** retained phase manifest/lock/config/generated snapshot files match their recorded SHA-256 hashes.
- All four npm packages are exactly `0.10.0` in baseline/candidate/rollback, with public npm source/integrity evidence.
- All three Rust crates resolve to `0.10.0 → 0.10.1 → 0.10.0`, with crates.io and lock checksum evidence.
- Actual generated Node stdio and Bun FFI probes each return `repeat: 3`; rollback contract hash and outputs equal mutated baseline.
- Node error propagation reports `transport.error` containing Rust error text; Bun reports `command.invalid_args`. Final docs accurately avoid claiming typed domain-error/code equivalence or event unsubscribe acceptance.

## Final documentation and evidence boundaries

Final EN/KO instructions explain CLI-compatible caret manifest strings, exact install inputs, frozen locks and exact installed-version enforcement. The status report distinguishes original scaffold and extended fixture, source CLI verification and published CLI use, local execution and the unexecuted manual workflow, unresolved GUI/mobile/evaluator/duration requirements, and the CLI patch that is prepared but unpublished. Earlier failed attempts are classified separately and retain their original paths/hashes.

Raw logs and snapshots remain at local temporary paths; the durable JSON preserves observations and hashes but does not make those raw files permanent. Native binaries were intentionally removed on successful scratch cleanup and are hash-only evidence. The status report explicitly states both limitations.

## Review limitations

No full network/native gate, remote workflow dispatch, package publication, commit, merge, external consumer change, GUI/mobile/device evaluation or long-duration test was performed by this reviewer. Broader CLI/build/docs/freshness gate results in the final status are parent-owned validation, not rerun here. Remote main/registry audit claims were checked for consistency with supplied evidence and were not independently re-queried. No external evaluator, G1/G2, release or 1.0 completion is inferred from this review.
