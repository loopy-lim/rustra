# Dev Tier 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 개발 중 동적 표면(invokeLoose·열린 디바이스 토큰·빠른 게이트)을
습격 없이 추가하고, 릴리스 벽은 doctor 검사와 release 패닉으로 유지한다.

**Architecture:** 엔진의 기존 동적 체인(이름 기반 invoke → live schema →
Tier 3) 위에 명명된 표면만 얹고, 디바이스 카탈로그를 schema.json
`deviceCapabilities`로 단일소싱해 CLI 수동 미러를 삭제한다. debug 빌드는
미지 토큰을 경고 후 수용하고 release 빌드는 패닉 — fail-closed는 doctor
`codegen.device_catalog` 검사로 이동.

**Tech Stack:** Rust(crates/rustra) · TypeScript(@rustra/types,
@rustra/cli) · bun test + node:test

**Spec:** `docs/plans/2026-09-08-dev-tier-design.md`

## Global Constraints

- 커밋은 한국어 Conventional Commits. **changeset 생성 금지**(사용자
  승인 게이트 — 스펙 changeset 절의 초안만 근거).
- 선언 없는 패키지의 schema.json/devices.ts는 **바이트 불변**(events
  관례). 카탈로그 필드·마커 주석은 미지 토큰/선언 존재 시에만.
- 커밋 직전 staged 파일 prettier/rustfmt/eslint --fix(lefthook pre-commit
  자동).
- 문서는 en/ko 미러 + `docs/README.md`·`README.ko.md` 인덱스 등록.
- 테스트 파일 관례: node:test + node:assert/strict, ESM, 새 의존성 없음.
- `packages/types` 신규 테스트 파일은 `packages/types/package.json`
  `test` 스크립트 목록에도 등록해야 umbrella `bun run test` 가 잡는다.
- 게이트 배터리(Task 9) 전에 umbrella `bun run test` 는 dist-ts +
  release cdylib 선빌드 필요(tsc + `cargo build --release -p
rustra-calculator-example`) — 스테일이면 node --test 영구 정지.

---

### Task 1: `invokeLoose` 표면 (@rustra/types)

**Files:**

- Create: `packages/types/src/invoke-loose.ts`
- Test: `packages/types/src/invoke-loose.test.ts`
- Modify: `packages/types/src/index.ts` (re-export), `packages/types/package.json:25` (test 스크립트 목록)

**Interfaces:**

- Consumes: `EngineClient`, `InvokeOptions` (`packages/types/src/public.ts:36`)
- Produces: `invokeLoose<T = unknown>(client: EngineClient, command: string, args?: unknown, options?: InvokeOptions): Promise<T>` — Task 7 예제 e2e가 소비.

- [x] **Step 1: 실패 테스트 작성**

```ts
// invokeLoose 단위 테스트 — 동적 티어의 명명된 저작 표면(Dev Tier A절).
// 저장소 표준(node:test + node:assert/strict, ESM) 사용.
import assert from 'node:assert/strict';
import test from 'node:test';
import { invokeLoose } from './invoke-loose.js';
import type { EngineClient } from './public.js';

test('invokeLoose delegates to the engine by command name', async () => {
  const calls: Array<{ command: string; args: unknown }> = [];
  const client: EngineClient = {
    async invoke<T>(command: string, args?: unknown): Promise<T> {
      calls.push({ command, args });
      return { os: 'darwin' } as T;
    },
  };
  const out = await invokeLoose<{ os: string }>(client, 'platformNativeInfo');
  assert.equal(out.os, 'darwin');
  assert.deepEqual(calls, [{ command: 'platformNativeInfo', args: undefined }]);
});

test('invokeLoose forwards args and options untouched', async () => {
  const controller = new AbortController();
  const seen: { args?: unknown; signal?: AbortSignal }[] = [];
  const client: EngineClient = {
    async invoke<T>(
      command: string,
      args?: unknown,
      options?: { signal?: AbortSignal },
    ): Promise<T> {
      seen.push({ args, signal: options?.signal });
      return 42 as T;
    },
  };
  const value = await invokeLoose<number>(
    client,
    'clamp',
    { value: 1 },
    {
      signal: controller.signal,
    },
  );
  assert.equal(value, 42);
  assert.deepEqual(seen[0]?.args, { value: 1 });
  assert.equal(seen[0]?.signal, controller.signal);
});

test('invokeLoose defaults the result type to unknown', async () => {
  const client: EngineClient = {
    async invoke<T>(command: string): Promise<T> {
      return { anything: true } as T;
    },
  };
  const out: unknown = await invokeLoose(client, 'echoGroups');
  assert.deepEqual(out, { anything: true });
});
```

- [x] **Step 2: 테스트 실패 확인**

Run: `cd packages/types && bun test src/invoke-loose.test.ts`
Expected: FAIL — `invoke-loose.js` 모듈 없음.

- [x] **Step 3: 구현**

```ts
/**
 * 느슨한 커맨드 호출 — 개발 중 프로토타이핑의 공식 표면 (Dev Tier A절).
 *
 * 새 매커니즘이 아니라 명명된 위임이다 — 엔진의 이름 기반 `invoke`가 정적
 * 코덱 fast-path → live schema commandId 조회 → Tier 3(JSON) 폴백 체인을
 * 이미 판정한다(rkyv 엔진은 `rkyv-engine-async.ts`, json 엔진은 JSON 경로).
 * Rust 핸들러를 debug 빌드에서 런타임 등록한 뒤 코드젠 없이 곧장 호출할 때
 * 쓴다. 정적 승격은 평소처럼 `rustra codegen` — 반환 타입 기본값은
 * `unknown`, 호출자가 좁힌다.
 */
import type { EngineClient, InvokeOptions } from './public.js';

export function invokeLoose<T = unknown>(
  client: EngineClient,
  command: string,
  args?: unknown,
  options?: InvokeOptions,
): Promise<T> {
  return client.invoke<T>(command, args, options);
}
```

`index.ts`에 `export { invokeLoose } from './invoke-loose.js';` 추가
(device-status re-export 근처, `export *` 가 이미 public.js 를 덮으므로
단일 re-export).

- [x] **Step 4: 테스트 통과 확인**

Run: `cd packages/types && bun test src/invoke-loose.test.ts`
Expected: 3 pass.

- [x] **Step 5: test 스크립트 등록 + api-surface 갱신**

`packages/types/package.json` `test` 목록에 `src/invoke-loose.test.ts`
추가. 이후 루트에서:

Run: `bun run --cwd packages/types build && node scripts/api-surface.mjs --update`
Expected: snapshot에 `invokeLoose` 추가.

- [x] **Step 6: Commit**

```bash
git add packages/types/src/invoke-loose.ts packages/types/src/invoke-loose.test.ts packages/types/src/index.ts packages/types/package.json packages/types/dist api-surface/snapshot.json
git commit -m "feat(types): invokeLoose 동적 티어 저작 표면 — 엔진 이름 기반 invoke의 명명된 위임"
```

---

### Task 2: 카탈로그 스키마 기록 (crates/rustra)

**Files:**

- Modify: `crates/rustra/src/package_schema.rs:110-141` (`schema` 함수)
- Test: `crates/rustra/src/internal_tests/builder_devices_tests.rs`

**Interfaces:**

- Produces: schema.json 최상위 `deviceCapabilities: string[]`(선언 있는
  패키지 한정, `DeviceCapability::ALL` 순서) — Task 4 CLI 렌더러가 소비.

- [x] **Step 1: 실패 테스트 작성** — `builder_devices_tests.rs` 말단에:

```rust
// ── 카탈로그 단일소싱 — 최상위 deviceCapabilities (Dev Tier B절) ──

/// 선언이 있으면 카탈로그 전체(ALL)가 최상위에 기록된다 — CLI 가 이 필드를
/// 단일 소싱해 수동 미러 없이 정렬·검증한다.
#[test]
fn schema_records_device_catalog_when_declared() {
    let schema = devices_package().live_schema();
    let catalog = schema["deviceCapabilities"]
        .as_array()
        .expect("declared package records the catalog");
    assert_eq!(catalog.len(), 21);
    assert_eq!(catalog[0], "camera");
    assert!(catalog.iter().any(|token| token == "bluetooth"));
}

/// 선언이 없으면 미기록 — 기존 패키지의 schema.json/계약 해시 불변(events 관례).
#[test]
fn schema_omits_device_catalog_when_undeclared() {
    let package = Package::builder("example.devices")
        .command("scan_tags", |input: serde_json::Value| {
            Ok::<_, RustraError>(input)
        })
        .build();
    let schema = package.live_schema();
    assert!(
        schema.get("deviceCapabilities").is_none(),
        "undeclared package must not record a catalog: {}",
        schema
    );
}
```

- [x] **Step 2: 테스트 실패 확인**

Run: `cargo test -p rustra schema_records_device_catalog -- --exact`
Expected: FAIL — catalog 없음.

- [x] **Step 3: 구현** — `schema` 함수의 events insert 뒤, `root` 반환 앞:

```rust
        // (Dev Tier B절) 디바이스 역량 카탈로그 단일소싱 — 선언이 있는 패키지에
        // 한해 카탈로그 전체(ALL, 선언 순)를 최상위에 기록한다. CLI(generate-devices)
        // 가 이 필드로 정렬·검증하며 수동 미러를 유지하지 않는다. 선언 없는
        // 패키지는 미기록(events 관례 — 기존 schema.json 바이트 불변).
        if state
            .commands
            .values()
            .any(|command| !command.device_requirements.is_empty())
        {
            let catalog: Vec<&str> = crate::device_capabilities::DeviceCapability::ALL
                .iter()
                .map(|capability| capability.as_str())
                .collect();
            root.as_object_mut()
                .expect("root is an object")
                .insert("deviceCapabilities".into(), json!(catalog));
        }
```

- [x] **Step 4: 테스트 통과 확인**

Run: `cargo test -p rustra builder_devices`
Expected: 전부 pass(기존 바이트 불변 테스트 포함).

- [x] **Step 5: Commit**

```bash
git add crates/rustra/src/package_schema.rs crates/rustra/src/internal_tests/builder_devices_tests.rs
git commit -m "feat(rustra): 디바이스 카탈로그를 schema.json deviceCapabilities로 단일소싱 — 선언 패키지 한정 조건부 기록"
```

---

### Task 3: debug 빌드 토큰 관대화 (crates/rustra)

**Files:**

- Modify: `crates/rustra/src/builder_devices.rs:38-55`, `crates/rustra/src/device_capabilities.rs:13-15`(모듈 문서)
- Test: `crates/rustra/src/internal_tests/builder_devices_tests.rs:143-154`

**Interfaces:**

- Produces: debug 빌드에서 카탈로그 밖 토큰 수용(스키마로 흐름) — Task 4
  렌더러·Task 5 doctor가 소비. release 빌드 패닉 불변.

- [x] **Step 1: 테스트 재작성** — 기존
      `command_devices_panics_on_catalog_outside_token`을 프로파일 분기로:

```rust
/// Dev Tier C절 — debug 빌드는 카탈로그 밖 토큰을 경고와 함께 수용한다
/// (프로토타이핑). 선언은 스키마 devices 로 흐르고 doctor 가 릴리스 벽이 된다.
#[test]
#[cfg(debug_assertions)]
fn command_devices_accepts_unknown_token_in_debug_builds() {
    let package = Package::builder("example.devices")
        .command("scan_tags", |input: serde_json::Value| {
            Ok::<_, RustraError>(input)
        })
        .command_devices("scan_tags", &[DeviceCapability::new("nfc-legacy-reader")])
        .build();
    let state = package
        .state
        .read()
        .unwrap_or_else(|poisoned| poisoned.into_inner());
    assert_eq!(
        state
            .commands
            .get("scan_tags")
            .unwrap()
            .device_requirements[0]
            .as_str(),
        "nfc-legacy-reader"
    );
    // 카탈로그는 ALL 그대로 — 미지 토큰이 카탈로그에 섞이지 않는다.
    let schema = package.live_schema();
    assert_eq!(schema["deviceCapabilities"].as_array().unwrap().len(), 21);
    assert_eq!(schema["commands"][0]["devices"][0], "nfc-legacy-reader");
}

/// 릴리스 빌드 벽 — 카탈로그 밖 토큰은 등록 시점 패닉(doctor 검사와 이중).
#[test]
#[cfg(not(debug_assertions))]
#[should_panic(expected = "unknown device capability 'camra'")]
fn command_devices_panics_on_catalog_outside_token() {
    let _ = Package::builder("example.devices")
        .command("scan_tags", |input: serde_json::Value| {
            Ok::<_, RustraError>(input)
        })
        .command_devices("scan_tags", &[DeviceCapability::new("camra")])
        .build();
}
```

- [x] **Step 2: 분기 테스트 실패 확인(debug)**

Run: `cargo test -p rustra command_devices_accepts_unknown -- --exact`
Expected: FAIL — 현행 패닉.

- [x] **Step 3: 구현** — `command_devices` 검증 루프의 카탈로그 판정 교체:

```rust
        for (index, capability) in devices.iter().enumerate() {
            if !crate::device_capabilities::DeviceCapability::is_valid(capability.as_str()) {
                // Dev Tier C절 — debug 빌드는 프로토타이핑을 위해 경고 후 수용,
                // release 빌드는 패닉(계약 벽). 릴리스 벽은 doctor
                // codegen.device_catalog 검사와 이중으로 유지된다.
                #[cfg(debug_assertions)]
                {
                    eprintln!(
                        "rustra: command_devices('{name}'): device capability '{}' is outside the \
                         DeviceCapability::ALL catalog — accepted in debug builds only (release \
                         builds panic; rustra doctor reports it)",
                        capability.as_str()
                    );
                }
                #[cfg(not(debug_assertions))]
                panic!(
                    "command_devices('{name}'): unknown device capability '{}' — must be one of \
                     DeviceCapability::ALL",
                    capability.as_str()
                );
            }
```

같이 갱신: (1) `command_devices` doc의 패닉 목록 — "토큰이 카탈로그 밖인
경우" 항목에 "(release 빌드 — debug 빌드는 경고 후 수용, Dev Tier)"
후缀. (2) `device_capabilities.rs` 모듈 문서 14행 "카탈로그 밖 토큰은
등록 시점에 패닉" → "release 빌드에서는 등록 시점에 패닉하고 debug
빌드에서는 경고 후 수용한다(Dev Tier — 릴리스 벽은 doctor 검사와
이중)".

- [x] **Step 4: 테스트 통과 확인**

Run: `cargo test -p rustra builder_devices && cargo test -p rustra device_capabilities`
Expected: 전부 pass.

- [x] **Step 5: Commit**

```bash
git add crates/rustra/src/builder_devices.rs crates/rustra/src/device_capabilities.rs crates/rustra/src/internal_tests/builder_devices_tests.rs
git commit -m "feat(rustra): debug 빌드 카탈로그 밖 디바이스 토큰 수용 — release 패닉 유지, 릴리스 벽은 doctor로 이중화"
```

---

### Task 4: CLI 렌더러 카탈로그 소싱 + 미지 토큰 렌더 (@rustra/cli)

**Files:**

- Modify: `packages/cli/src/schema.ts:68-92` (PackageSchema), `packages/cli/src/generate-devices.ts`(전면)
- Test: `packages/cli/src/generate.test.ts:2451-2582`

**Interfaces:**

- Consumes: schema `deviceCapabilities?: string[]` (Task 2)
- Produces: 미지 토큰 포함 유니언/상수 + 마커 주석. 카탈로그 부재 시 throw.

- [x] **Step 1: fixture 갱신 + 실패 테스트** — `generate.test.ts`:

`deviceSchema` fixture에 카탈로그 추가(선언 순 = Rust ALL 순서 발췌):

```ts
const deviceSchema: PackageSchema = {
  packageId: 'example.scanner',
  // Rust DeviceCapability::ALL 카탈로그가 schema.json deviceCapabilities로
  // 기록된다(Task 2) — 렌더러의 단일 소스(수동 미러 폐지, Dev Tier B절).
  deviceCapabilities: [
    'camera',
    'microphone',
    'geolocation',
    'notifications',
    'clipboard-read',
    'clipboard-write',
    'wifi',
    'bluetooth',
    'battery',
    'nfc',
    'biometric',
    'haptics',
    'flashlight',
    'contacts',
    'calendar',
    'photo-library',
    'motion',
    'usb',
    'serial',
    'network-state',
    'screen-brightness',
  ],
  commands: [/* 기존 그대로 */],
};
```

기존 `generateDevicesTs fails loud on out-of-catalog tokens` 테스트를
교체(새 동작 2종):

```ts
test('generateDevicesTs renders out-of-catalog tokens with a dev marker', async () => {
  const { generateDevicesTs } = await import('./generate.js');
  // debug 빌드(Rust)가 수용한 카탈로그 밖 토큰 — throw 대신 렌더 대상.
  // 정렬: 카탈로그 순(known) 뒤 미지 토큰 알파벳순 — 선언 순서와 무관하게 결정적.
  const dev: PackageSchema = {
    ...deviceSchema,
    commands: [
      {
        ...deviceSchema.commands[0]!,
        devices: ['bluetooth', 'nfc-legacy-reader', 'alpha-hw'],
      },
    ],
  };
  const out = generateDevicesTs(dev);
  assert.ok(
    out.includes(
      "export type RustraDeviceCapability = 'bluetooth' | 'alpha-hw' | 'nfc-legacy-reader';",
    ),
    out,
  );
  assert.ok(
    out.includes(
      "export const SCAN_TAGS_DEVICES: readonly RustraDeviceCapability[] = ['bluetooth', 'alpha-hw', 'nfc-legacy-reader'];",
    ),
    out,
  );
  // 마커 주석 — 미지 토큰 목록 + doctor 릴리스 벽 안내.
  assert.match(out, /카탈로그 밖 토큰 2개/u);
  assert.match(out, /'alpha-hw', 'nfc-legacy-reader'/u);
  assert.match(out, /codegen\.device_catalog/u);
});

test('generateDevicesTs throws when declared devices lack the schema catalog', async () => {
  const { generateDevicesTs } = await import('./generate.js');
  // 구버전 rustra 스키마(카탈로그 필드 없음) + 선언 — fail-closed 재생성 안내.
  const legacy: PackageSchema = { ...deviceSchema, deviceCapabilities: undefined };
  assert.throws(
    () => generateDevicesTs(legacy),
    (error: unknown) => error instanceof Error && error.message.includes('deviceCapabilities'),
    'legacy schema without the catalog must fail loud',
  );
});
```

`schema.ts` PackageSchema에 필드 추가(events 뒤):

```ts
  /**
   * (Dev Tier B절) Rust `DeviceCapability::ALL` 카탈로그 — 선언이 있는
   * 패키지에 한해 기록된다. `devices.ts` 렌더러의 단일 소스(수동 미러
   * 폐지). 선언이 없으면 섹션 자체가 없다(하위호환).
   */
  deviceCapabilities?: string[];
```

- [x] **Step 2: 테스트 실패 확인**

Run: `cd packages/cli && bun test src/generate.test.ts`
Expected: FAIL — 신규 2종(미지 렌더·카탈로그 부재 throw) 및 fixture
소싱.

- [x] **Step 3: 렌더러 재작성** — `generate-devices.ts`:

- `DEVICE_CAPABILITY_CATALOG` 상수·`CATALOG_ORDER` 삭제.
- `generateDevicesTs` 머리에 카탈로그 확보(선언 존재 시):

```ts
const catalog = schema.deviceCapabilities ?? [];
if (catalog.length === 0) {
  throw new Error(
    'Invalid schema: schema.json lacks the top-level deviceCapabilities catalog while ' +
      'commands declare devices — regenerate schema.json with a current rustra ' +
      '(the catalog is emitted alongside device declarations)',
  );
}
const catalogOrder = new Map(catalog.map((token, index) => [token, index]));
```

- `sortedCatalogTokens` 교체(throw 제거, known/unknown 분리 정렬):

```ts
/** 커맨드 하나의 토큰 정렬 — 카탈로그 순(known) 뒤 미지 토큰 알파벳순.
 *  미지 토큰은 debug 빌드가 수용한 선언이다(Dev Tier C절) — 마커 대상. */
function sortedCatalogTokens(
  command: CommandSchema,
  catalogOrder: Map<string, number>,
): { tokens: string[]; unknownTokens: string[] } {
  const seen = new Set<string>();
  const tokens: string[] = [];
  for (const token of command.devices ?? []) {
    if (seen.has(token)) continue;
    seen.add(token);
    tokens.push(token);
  }
  const known = tokens.filter((token) => catalogOrder.has(token));
  const unknownTokens = tokens.filter((token) => !catalogOrder.has(token));
  known.sort((a, b) => (catalogOrder.get(a) ?? 0) - (catalogOrder.get(b) ?? 0));
  unknownTokens.sort();
  return { tokens: [...known, ...unknownTokens], unknownTokens };
}
```

- 유니언 렌더 뒤 마커(unknownTokens 합집합이 있을 때만 — 바이트 불변):

```ts
const unknownAll = [...new Set(surfaces.flatMap((s) => s.unknownTokens))].sort();
if (unknownAll.length > 0) {
  output += `// 카탈로그 밖 토큰 ${unknownAll.length}개 — debug 빌드에서만 등록 가능하다(release 빌드는\n`;
  output += `// 패닉, rustra doctor codegen.device_catalog 검사가 릴리스 벽이다): ${unknownAll
    .map((token) => `'${token}'`)
    .join(', ')}\n`;
}
```

파일 머리 주석 갱신: 미러 폐지·스키마 소싱 명시. `byCatalogOrder` 삭제.

- [x] **Step 4: 테스트 통과 확인**

Run: `cd packages/cli && bun test src/generate.test.ts`
Expected: 전부 pass(골든·충돌·backcompat 포함).

- [x] **Step 5: Commit**

```bash
git add packages/cli/src/schema.ts packages/cli/src/generate-devices.ts packages/cli/src/generate.test.ts
git commit -m "feat(cli): devices 렌더러를 스키마 deviceCapabilities로 단일소싱 — 수동 미러 폐지, 미지 토큰 dev 마커 렌더"
```

---

### Task 5: doctor `codegen.device_catalog` 검사 (@rustra/cli)

**Files:**

- Modify: `packages/cli/src/doctor-checks.ts`(freshness 블록 뒤, dev.wasm 앞)
- Test: `packages/cli/src/doctor.test.ts`

**Interfaces:**

- Consumes: schema.json `commands[].devices` + `deviceCapabilities` (Task 2·4)
- Produces: 검사 `codegen.device_catalog` — pass/fail/warn/skip.

- [x] **Step 1: 실패 테스트 작성** — `doctor.test.ts` freshness 절 뒤:

```ts
// ── 디바이스 토큰 릴리스 벽 (codegen.device_catalog) ──────────────────────────
//
// debug 빌드가 수용한 카탈로그 밖 토큰을 릴리스 전에 보고한다(Dev Tier C절).
// release 빌드는 등록 시점 패닉 — doctor 가 JS 쪽 벽으로 이중화한다.

function withDeviceProject(schema: object, callback: (root: string) => void): void {
  withProject(
    {
      'rustra.json': JSON.stringify({
        schema: './generated/schema.json',
        output: './generated',
        node: { rustManifest: './Cargo.toml' },
      }),
      'Cargo.toml': CARGO_TOML,
      'generated/schema.json': JSON.stringify(schema),
    },
    callback,
  );
}

test('doctor passes device tokens inside the catalog', () => {
  withDeviceProject(
    {
      deviceCapabilities: ['camera', 'bluetooth'],
      commands: [{ name: 'scan_tags', devices: ['camera'] }],
    },
    (root) => {
      const report = collectDoctorReport(
        options(join(root, 'rustra.json')),
        metadataRunner(root, []),
      );
      const verdict = report.checks.find((check) => check.id === 'codegen.device_catalog');
      assert.equal(verdict?.status, 'pass');
    },
  );
});

test('doctor fails on device tokens outside the catalog', () => {
  withDeviceProject(
    {
      deviceCapabilities: ['camera', 'bluetooth'],
      commands: [{ name: 'scan_tags', devices: ['camera', 'nfc-legacy-reader'] }],
    },
    (root) => {
      const report = collectDoctorReport(
        options(join(root, 'rustra.json')),
        metadataRunner(root, []),
      );
      const verdict = report.checks.find((check) => check.id === 'codegen.device_catalog');
      assert.equal(verdict?.status, 'fail');
      assert.equal(verdict?.required, true);
      assert.match(verdict?.summary ?? '', /nfc-legacy-reader/);
    },
  );
});

test('doctor warns when declared devices lack the catalog field', () => {
  withDeviceProject({ commands: [{ name: 'scan_tags', devices: ['camera'] }] }, (root) => {
    const report = collectDoctorReport(
      options(join(root, 'rustra.json')),
      metadataRunner(root, []),
    );
    const verdict = report.checks.find((check) => check.id === 'codegen.device_catalog');
    assert.equal(verdict?.status, 'warn');
    assert.equal(verdict?.required, false);
  });
});

test('doctor skips the catalog check without device declarations', () => {
  withDeviceProject({ deviceCapabilities: ['camera'], commands: [{ name: 'add' }] }, (root) => {
    const report = collectDoctorReport(
      options(join(root, 'rustra.json')),
      metadataRunner(root, []),
    );
    const verdict = report.checks.find((check) => check.id === 'codegen.device_catalog');
    assert.equal(verdict?.status, 'skip');
  });
});
```

- [x] **Step 2: 테스트 실패 확인**

Run: `cd packages/cli && bun test src/doctor.test.ts`
Expected: FAIL — 검사 부재(find undefined).

- [x] **Step 3: 구현** — `collectConfigChecks` freshness 블록 뒤:

```ts
// Dev Tier C절 — 디바이스 토큰 릴리스 벽. debug 빌드가 수용한 카탈로그 밖
// 토큰을 릴리스 전에 보고한다(release 빌드는 등록 시점 패닉 — 이중 벽).
// schema.json 이 없으면 codegen.schema_output warn 이 이미 담당하므로 여기서는
// 침묵한다(이중 보고 금지 관례).
if (schemaPath && existsSync(schemaPath)) {
  let verdict: DoctorCheck;
  try {
    const schema = JSON.parse(readFileSync(schemaPath, 'utf8')) as {
      deviceCapabilities?: unknown;
      commands?: Array<{ name?: unknown; devices?: unknown }>;
    };
    const declared = (schema.commands ?? []).filter(
      (command) => Array.isArray(command.devices) && (command.devices as unknown[]).length > 0,
    );
    const catalog = schema.deviceCapabilities;
    if (declared.length === 0) {
      verdict = check(
        'codegen.device_catalog',
        'skip',
        false,
        'No device declarations — catalog check not applicable',
      );
    } else if (!Array.isArray(catalog)) {
      verdict = check(
        'codegen.device_catalog',
        'warn',
        false,
        'schema.json lacks the deviceCapabilities catalog while commands declare devices',
        undefined,
        ['Run rustra codegen --config rustra.json with a current rustra'],
      );
    } else {
      const known = new Set(catalog.filter((token): token is string => typeof token === 'string'));
      const unknown = [
        ...new Set(
          declared.flatMap((command) =>
            (command.devices as unknown[]).filter(
              (token): token is string => typeof token === 'string' && !known.has(token),
            ),
          ),
        ),
      ].sort();
      verdict =
        unknown.length === 0
          ? check(
              'codegen.device_catalog',
              'pass',
              false,
              `All declared device tokens are in the rustra catalog (${known.size} tokens)`,
            )
          : check(
              'codegen.device_catalog',
              'fail',
              true,
              `Device tokens outside the rustra catalog: ${unknown
                .map((token) => `'${token}'`)
                .join(', ')}`,
              'Debug builds accept catalog-outside tokens; release builds panic at registration',
              [
                'Rename to a catalog token or extend the rustra catalog (crates/rustra/src/device_capabilities.rs)',
              ],
            );
    }
  } catch (error) {
    verdict = check(
      'codegen.device_catalog',
      'fail',
      true,
      'schema.json could not be parsed for the device catalog check',
      error instanceof Error ? error.message : String(error),
    );
  }
  checks.push(verdict);
}
```

- [x] **Step 4: 테스트 통과 확인**

Run: `cd packages/cli && bun test src/doctor.test.ts`
Expected: 전부 pass. `cd packages/cli && bun run test:compile`도 통과.

- [x] **Step 5: Commit**

```bash
git add packages/cli/src/doctor-checks.ts packages/cli/src/doctor.test.ts
git commit -m "feat(cli): doctor codegen.device_catalog 검사 — 미지 디바이스 토큰 릴리스 벽(pass/fail/warn/skip)"
```

---

### Task 6: `test:fast` 게이트 프로파일

**Files:**

- Modify: `package.json`(루트 scripts)

**Interfaces:**

- Produces: `bun run test:fast` — 문서(Task 8)가 참조.

- [x] **Step 1: 스크립트 추가** — `test` 정의 뒤에:

```json
    "test:fast": "cargo check --workspace -q && tsc -p examples/calculator/tsconfig.json && bun run --cwd packages/cli test",
```

- [x] **Step 2: 동작 확인**

Run: `bun run test:fast`
Expected: 3단계 전부 exit 0(신규 검사·렌더러 포함).

- [x] **Step 3: Commit**

```bash
git add package.json
git commit -m "chore(repo): test:fast 개발 루프 게이트 — cargo check + calculator tsc + cli 유닛 (풀 배터리는 CI/PR 유지)"
```

---

### Task 7: 예제 재생성 + e2e

**Files:**

- Create: `examples/calculator/ts/loose-invoke.test.ts`
- Regenerate: `examples/calculator/generated/`(schema.json·contract.ts — devices.ts 바이트 불변 확인)

**Interfaces:**

- Consumes: `invokeLoose` (Task 1), 카탈로그 스키마(Task 2), 렌더러(Task 4)

- [x] **Step 1: e2e 테스트 작성**

```ts
// invokeLoose e2e — 동적 티어 저작 표면을 글로벌 클라이언트 회로로 검증.
import assert from 'node:assert/strict';
import test from 'node:test';
import { invokeLoose } from '@rustra/types';

test('invokeLoose prototypes a command by name without a generated client', async () => {
  const calls: string[] = [];
  const client = {
    async invoke<T>(command: string): Promise<T> {
      calls.push(command);
      return { os: 'darwin' } as T;
    },
  };
  const out = await invokeLoose<{ os: string }>(client, 'platformNativeInfo');
  assert.equal(out.os, 'darwin');
  assert.deepEqual(calls, ['platformNativeInfo']);
});
```

- [x] **Step 2: 코드젠 재생성**

Run: `cd examples/calculator && bun run codegen`
Expected: schema.json에 `deviceCapabilities` 21종 추가, contract.ts 해시
갱신, **devices.ts·commands.ts·types.ts 바이트 불변**(`git diff --stat`로
확인 — devices.ts가 바뀌면 렌더러 회귀).

- [x] **Step 3: bun 예제 테스트 통과 확인**

Run: 루트에서 `cargo build -p rustra-calculator-example -q && bun test examples/calculator/ts/devices.test.ts examples/calculator/ts/loose-invoke.test.ts`
Expected: 전부 pass.

- [x] **Step 4: Commit**

```bash
git add examples/calculator/ts/loose-invoke.test.ts examples/calculator/generated
git commit -m "feat(example): calculator invokeLoose e2e + 카탈로그 스키마 재생성 반영"
```

---

### Task 8: 문서 en/ko + 인덱스

**Files:**

- Create: `docs/dev-tier.md`, `docs/dev-tier.ko.md`
- Modify: `docs/README.md`, `docs/README.ko.md`

**Interfaces:**

- Consumes: Task 1-6 전부(문서화 대상)

- [x] **Step 1: 가이드 작성** — ko판 기준 구성(en판은 미러):

1. **동적 티어 개념** — 개발 중 동적/릴리스 정적 3문단 + 4구성요소 표.
2. **`invokeLoose` 프로토타이핑 루프** — Rust 핸들러(debug 런타임 등록
   `Package::register` 또는 매크로) → `cargo build` → `invokeLoose` 즉석
   호출. 코드젠·tsc 불필요(live schema가 commandId 제공, Tier 3 경로).
   정적 승격: `rustra codegen`. Tier 3 비용 주의(고빈도 명령은 승격 권장).
3. **디바이스 토큰 실험** — debug 빌드 미지 토큰 경고 후 수용 →
   devices.ts 마커 → `rustra doctor` fail → 해결(카탈로그 토큰 교체 또는
   rustra 카탈로그 확장). release 빌드 패닉 고지.
4. **게이트 프로파일** — `test:fast` vs 풀 배터리 표(무엇이 어느 시점에).
5. **범위 밖** — `codegen --from-live` 후속 슬라이스 안내.

- [x] **Step 2: 인덱스 등록** — 양쪽 README Reading Paths(Library Users
      목록 끝) + Full Document List 표 행 추가.

- [x] **Step 3: 문서 게이트 통과 확인**

Run: `bun run test:docs`
Expected: pass(devices.ts 인용 리전 바이트 불변 — Task 7에서 확인됨).

- [x] **Step 4: Commit**

```bash
git add docs/dev-tier.md docs/dev-tier.ko.md docs/README.md docs/README.ko.md
git commit -m "docs: dev-tier 가이드 en/ko — 동적 티어 저작 루프·미지 토큰 벽·게이트 프로파일 + 인덱스"
```

---

### Task 9: 전체 게이트 배터리

- [x] **Step 1: 사전 빌드** — `bun run build`(dist-ts·dist) 및
      `cargo build --release -p rustra-calculator-example -q`.
- [x] **Step 2: Rust** — `cargo fmt --all -- --check && cargo clippy
--all-targets -- -D warnings && cargo test --workspace`.
- [x] **Step 3: TS 유닛/umbrella** — `bun run test`(release-tools·types·
      ts:bun·packages·cli·codec bench·bench gate).
- [x] **Step 4: 노드 경로** — `bun run test:ts:node && bun run
test:ts:bun`.
- [x] **Step 5: 게이트 스크립트** — `bun run test:architecture && bun run
test:api-surface && bun run test:onboarding && bun run test:docs`.
- [x] **Step 6: 정리 커밋** — 게이트 과정에서 발생한 잔여 변동
      (스냅샷·포맷) 있으면 `chore(gate): …` 커밋. 없으면 생략.

## Self-Review 결과

- 스펙 §A→Task 1·7, §B→Task 2·4, §C→Task 3·4·5, §D→Task 6, §E→Task 8,
  §F→각 Task 스텝, changeset→Global Constraints(생성 금지). 커버 갭 없음.
- 타입 일치: `invokeLoose` 시그니처 Task 1=Task 7. `deviceCapabilities`
  이름 Task 2·4·5 동일. doctor 검사 id `codegen.device_catalog` Task 5·8
  동일.
- 바이트 불변 검증 스텝이 Task 2(undeclared)·4(마커 조건부)·7(git diff)
  에 있음.

## 실행 기록 (2026-09-08)

전 스텝 완료. 계획에서 벗어난 보강 2건:

- **Task 5 이후**: doctor-checks.ts가 457줄로 하드 상한(400)을 넘어
  architecture 게이트가 실패 — 검사 본체를 `doctor-device-catalog.ts`로
  분리하고 doctor-checks.ts는 위임만 남김(385줄). doctor.test.ts 4케이스는
  collectDoctorReport 경유라 무변경 통과.
- **Task 3**: debug 수용 테스트가 read 락을 잡은 채 live_schema() 를
  호출해 자기 교착 — 락 스코프 분리로 수정(계획서 코드는 수정본 기준).

최종 게이트: fmt/clippy/cargo test --workspace/umbrella test/ts:node/
ts:bun/architecture/api-surface/onboarding/docs/eslint/prettier 전부 통과.
changeset은 사용자 승인 게이트 대기(스펙 changeset 절 초안 참조).
